# Scope Panel - Raw Scoper Proposals

> Verbatim output of the three divergent scopers, before merge/convergence.
> Panel run 2026-08-19, wf_854b2b93-a5a - 3/3 scopers OK, 2/2 adversaries OK, no degraded lenses.

---

## 

```json
{
    "scoper":  "fit",
    "ok":  true,
    "fit":  {
                "verdict":  "reshape",
                "rationale":  "Two halves with opposite fits, and the request treats them as one.\n\nTHE READ HALF IS A CLEAN FIT, arguably the cleanest extension available. src/ootp_ai/contracts/tables.toml + field_map.toml are already a single declaration with three consumers (warehouse/ddl.py, tests/test_grain_contracts.py, the deferred catalog/generate.py), and contracts/policy.py::column_disposition (lines 164-184) already decides per column and fails closed. A gm_view emitter is a fourth consumer of the same declaration, written exactly as warehouse/ddl.py::create_all_statements is written, landed beside warehouse/load.py::ensure_tables (:169), quoted through warehouse/sql.py::quote_ident. MEASURED (I ran column_disposition over the shipped declaration): 8 tables, 96 columns, 94 RENDERABLE, 1 WITHHELD (bronze_name.name_category), 1 UNCERTAIN (bronze_league_event.real_sim_date).\n\nTHE WRITE HALF\u0027S CENTRAL CLAIM DOES NOT SURVIVE THE HARNESS. The thesis is \u0027enforcement by grant, not prose\u0027. .claude/agents/README.md:100-113 states the counter-fact in bold: \u0027This harness has no path-level permission system. Agent frontmatter tools: gates which tools an agent holds - that part is real and enforced - but nothing gates which paths those tools may touch.\u0027 So Open Question 2\u0027s first option (a Write/Edit grant scoped to gm/journal.jsonl) is unenforceable, and it puts the GM\u0027s pen in the same tree as .claude/agents/gm.md, FRONT_OFFICE.md and tests/. It would be prose protecting a LARGER capability than the prose it replaces. Reshape to structured-return plus an umpire-side lander: that keeps ADR 0017\u0027s pen-holding and makes append-only mechanically true, because the only writer is code that only appends.\n\nTHE QUERY HALF HAS NO VEHICLE IN THIS REPO TODAY. There is no .mcp.json and no .claude/settings.json (verified by recursive listing of .claude/ - only agents/ and skills/). The only shipped way a subagent executes SQL is a Bash grant, which hands the GM a shell, .env, the writable ootp schema and the game directory in one move - strictly worse than ADR 0017\u0027s status quo. So the enforceable version needs a project-scoped read-only MCP query tool bound to a restricted DSN. That is real, unnamed build work, and it is exactly where the request\u0027s enforcement claim either becomes true or does not.\n\nTWO SETTLED-ADR COLLISIONS THE REQUEST DOES NOT NAME. (1) ADR 0005 assigns \u0027gold (serving models for the front office)\u0027 to the dbt medallion; gm_view IS that gold layer arriving without dbt. ADR 0004\u0027s Notes (:89-98) leave dbt open - \u0027The decision comes due when the first dbt model is requested\u0027, with option 2 being \u0027Keep MySQL, drop dbt - hand-rolled SQL plus a thin runner\u0027. Generating views from contracts/ is option 2 by the back door, and CLAUDE.md forbids quiet divergence. (2) contracts/field_map.toml\u0027s name_category entry pre-registers this exact hand-off: it is filed category=\u0027rating-true\u0027 purely to do withhold-by-default duty for an unclassified byte, and its own comment says a distinct \u0027unclassified\u0027 category \u0027is a change to the serving gate\u0027s posture and belongs to whoever revisits ADR 0012, not to a loader\u0027. This request moves the gate; it is the addressee of that note, while also declaring 0012 explicitly out of scope.\n\nA THREE-VALUED POLICY DOES NOT MAP ONTO A TWO-VALUED SCHEMA. Disposition has RENDERABLE / UNCERTAIN / WITHHELD (policy.py:89-95). A view column either exists or does not. UNCERTAIN\u0027s whole mechanic is a RENDERING contract - render_with_uncertainty (:135) and uncertainty_banner (:151) force a banner onto a page - and a SQL view cannot force a banner. Exactly one column is UNCERTAIN today, so the cost of deciding is tiny; the decision is structural and must be made explicitly rather than by whoever writes the generator.\n\nNet: the infrastructure goes with the grain, the governance rewrite is legitimate and well-argued, and two of the three mechanisms the request names as enforcement are not enforceable as described. Reshape, not poor fit."
            },
    "goals":  [
                  "Replace the report wall with a SCHEMA boundary: the GM answers baseball questions by querying a gm_view schema generated from contracts/tables.toml + field_map.toml via contracts/policy.py::column_disposition, with withheld columns physically absent rather than declined by prose.",
                  "Make ADR 0012\u0027s withhold-by-default posture STRICTLY STRONGER at the handover, not merely preserved: the same fail-closed function that gates a report page gates the schema, plus a name-fragment backstop over the emitted SQL text and a live-schema diff against the declaration.",
                  "Give the GM durable memory it authors: a journal, dated triggers served back at a later sim date without anyone remembering, predictions it can be wrong about, and a revisable plan - so a per-invocation subagent graded on multi-year outcomes (ADR 0015) finally has a time horizon.",
                  "Make the reasoning record tamper-evident by a mechanism that runs in CI as it stands today - CI checks out at fetch-depth: 1 (.github/workflows/ci.yml:22), so any guard depending on git history is not currently runnable.",
                  "Retire the action economy as the GM\u0027s information model and convert gm/ledger.jsonl\u0027s adjudication role into an execution log keyed on disposition (proposed / executed / deferred / declined) with a mandatory reason, preserving declare-before-doing.",
                  "Land the ADR record with the repo\u0027s career-record discipline: supersede 0013/0016/0018/0019 and amend 0014/0017 by writing new ADRs and adding supersession blockquotes to the old ones, never deleting text - the pattern 0016:6-19 and 0017 already use.",
                  "Give explicit, written dispositions to the three pinned requests (first-sight Phases 10-11, gm-inbox, news-subscription-dial) so none is left silently re-framed."
              ],
    "non_goals":  [
                      "Any change to ADRs 0012, 0015, 0003, 0001, 0021 or 0006 (carried from the request). In particular: no \u0027unclassified\u0027 category is added to field_map.toml\u0027s vocabulary in this slice, even though the name_category entry invites it - that is a change to the serving gate\u0027s posture and ADR 0012 is out of scope.",
                      "Widening what the parser reads. No new .dat file, no new field. gm-inbox stays its own request, and the scouting.dat ratings slice named by reviews/spike-scouted-view.md stays unbuilt - so gm_view\u0027s withhold machinery is prospective insurance over a rating set that has not landed.",
                      "Building analytics, models or advisors FOR the GM. Directing that becomes the GM\u0027s job once this lands.",
                      "A condition-evaluating trigger engine. Dated triggers only.",
                      "Pulling dbt. ADR 0004\u0027s dbt question is FORCED by this work but is not answered by it; the scope records the divergence in 0004\u0027s Notes rather than resolving the adapter choice.",
                      "A path-scoped write sandbox for the GM. The harness cannot provide one (.claude/agents/README.md:100-113) and simulating one in prose would be a regression dressed as enforcement.",
                      "A warehouse retention policy, and any pruning of landed snapshots (ADR 0018\u0027s Costs section flags this as owed; it stays owed).",
                      "Reconstructing true ratings from observables. ADR 0014\u0027s surviving core stands: analytics over scouted inputs is in, regression toward the answer key is out.",
                      "Changing the ingest path, the parser, bronze landing or any of the eight declared tables. gm_view reads what is already there."
                  ],
    "acceptance_criteria":  [
                                "OFFLINE. `uv run pytest tests/test_gm_view.py -m \u0027not gamedata\u0027` is green: for every table in load_contracts(), the emitted view\u0027s column list equals exactly the columns whose column_disposition is not Disposition.WITHHELD. Pinned against the measured baseline - 96 declared columns in, 95 out, with bronze_name.name_category absent. Fed a synthetic Contracts carrying a category=\u0027rating-true\u0027 column, the emitter omits that column from the emitted SQL string.",
                                "OFFLINE. The view emitter refuses a Disposition member it does not handle, mirroring warehouse/ddl.py::_check_every_declared_type_is_renderable (:123) and contracts/policy.py::check_policy_covers (:187). A test that introduces an unhandled disposition asserts a raise, not a silent pass-through.",
                                "OFFLINE. No emitted view SQL string contains any fragment in contracts.policy.WITHHELD_NAME_FRAGMENTS (\u0027prone_\u0027, \u0027players_value\u0027, \u0027_talent_\u0027) - a text-level backstop over the generated statements, the same second-lock pattern column_disposition applies to column names at policy.py:180.",
                                "GAMEDATA. Connecting as the restricted gm_reader user: `SELECT * FROM ootp.bronze_player LIMIT 1` raises an access-denied error; `SHOW DATABASES` returns no schema other than gm_view and information_schema; a SELECT against a gm_view view succeeds. This is the criterion that proves the grant rather than the prose, and it must fail if ops/mysql-bootstrap.sql\u0027s grant is widened.",
                                "OFFLINE. `.claude/agents/gm.md`\u0027s YAML frontmatter `tools:` value equals an exact expected list, and the file body contains no \u0027Bash\u0027, \u0027Write\u0027 or \u0027Edit\u0027 token. This lands the guard already filed as a follow-up by first-sight IMPLEMENTATION_PLAN.md:1108 (\u0027the GM tool-grant guard test ... nothing under tests/ asserts it\u0027), extending tests/test_agent_contract.py, which today covers only data-engineer.md.",
                                "OFFLINE. Every append-only file under gm/ verifies as a hash chain: each entry carries a prev_hash over the canonical serialization of the entry before it, and a pure pytest recomputes the whole chain with NO git history and no network. The suite includes a fixture whose middle entry has been retroactively edited, and that fixture must go red. (Chosen because .github/workflows/ci.yml:22 pins fetch-depth: 1, so a merge-base prefix check cannot run today, and .gitattributes\u0027 merge=union does not preserve base-as-prefix anyway.)",
                                "OFFLINE. Trigger round-trip: a trigger written at sim date N with due date N+k is ABSENT from the assembled context at N+k-1, PRESENT at N+k and at N+k+1, and absent after it is closed with a disposition. Run against a temp store, no warehouse and no save.",
                                "OFFLINE. Every new gm/ append-only artifact states its grain in prose in gm/README.md AND is enforced by a uniqueness test on its declared key, and the two agree - the same prose-and-key agreement contracts/loader.py::_check_grain_matches_key (:476) enforces for the warehouse, applied to GM memory.",
                                "OFFLINE. `uv run pytest -m \u0027not gamedata\u0027` green with no MySQL and no OOTP install; `ruff check .`, `ruff format --check .`, `uv run mypy` clean. Specifically green: tests/test_repo_structure.py::test_every_adr_is_indexed and ::test_adrs_are_sequentially_numbered with the new ADRs numbered consecutively from 0022; tests/test_doc_links.py over the rewritten FRONT_OFFICE.md, .claude/agents/gm.md and gm/README.md; tests/test_no_leaks.py over the new .jsonl files (already inside its keep set at test_no_leaks.py:92).",
                                "OFFLINE. A doc guard asserts the retired rules survive ONLY as history: FRONT_OFFICE.md, .claude/agents/gm.md and gm/README.md contain no live statement of \u00276 actions per in-season week\u0027, \u0027commissioning a report costs an action\u0027, or \u0027you read reports, never the warehouse\u0027; and each superseded ADR file still exists and carries a supersession blockquote naming its successor, matching the shape already at 0016-gm-reads-reports-not-queries.md:6-19.",
                                "USER-RUN. A cold `gm` spawn answers a baseball question by issuing at least one query through its granted query tool, and its returned document names the view it read and the ingest_seq it resolved to. Marked user-run: \u0027answered it well\u0027 is a judgment the acceptance panel must not claim, per requests/feature-requests/README.md\u0027s user-run rule.",
                                "USER-RUN. The operator confirms by hand that OOTP-AI.lg\u0027s file set, sizes and modification times are unchanged after a full weekly cycle including GM queries and memory landing - ADR 0001, same posture as first-sight\u0027s AC21 (IMPLEMENTATION_PLAN.md:1106)."
                            ],
    "core_scope":  [
                       "TWO ADRs, not one, numbered 0022 and 0023 consecutively (tests/test_repo_structure.py::test_adrs_are_sequentially_numbered forbids gaps) and both indexed in docs/decisions/README.md. 0022 = the information model: supersedes 0013 and 0016, retires 0018/0019\u0027s pricing while preserving 0019\u0027s feed-vs-warehouse insight and its refusal loop, amends 0014 to sensor-vs-processing and amends 0017\u0027s no-DB foreclosure into a scoped grant. 0023 = the memory model: append-only GM authorship, the execution log, the mutable plan, and 0017\u0027s no-write foreclosure narrowed. Separate because they have separable reasoning and separable failure modes - 0017\u0027s own history shows one ADR carrying two decisions is what produced 0018 and 0019 as patches.",
                       "warehouse/views.py - a pure emitter beside warehouse/ddl.py, turning Contracts + column_disposition into CREATE OR REPLACE VIEW statements. It connects to nothing (ddl.py\u0027s docstring rule: \u0027Nothing in this module connects to anything\u0027), so its whole guard suite runs in CI. Plus ensure_views() beside warehouse/load.py::ensure_tables (:169) as the only thing that touches MySQL.",
                       "AN EXPLICIT RULING ON Disposition.UNCERTAIN AT THE SCHEMA BOUNDARY. The policy has three outcomes and a view has two. Recommendation: the column is PRESENT but its view alias carries the label (e.g. real_sim_date__unconfirmed), so the banner obligation survives into a surface that cannot render banners; the alternative is exclusion. Exactly one column is affected today (bronze_league_event.real_sim_date, epistemic=\u0027unconfirmed\u0027, validator=\u0027none\u0027). Whichever way it goes, the emitter must REFUSE an unhandled Disposition rather than default.",
                       "THE GRANT. ops/mysql-bootstrap.sql gains a gm_reader@localhost with SELECT on gm_view.* and nothing on ootp, ootp_dev or ootp_truth_real. .env.example gains its keys; config.py gains a settings dataclass resolved the same way MySQLSettings is (config.py:85-95), so nothing hardcodes a DSN and tests/test_no_leaks.py stays green.",
                       "THE QUERY VEHICLE, and Bash is disqualified. In scope: a project-scoped read-only MCP query tool bound to the gm_reader DSN, registered in a tracked .mcp.json (the repo\u0027s first), with .claude/agents/gm.md\u0027s tools: naming that tool and nothing else. This is the load-bearing new build - tool grants ARE harness-enforced (.claude/agents/README.md:106-107), which is the one enforcement lever this repo actually has. If it cannot be made to work, the read half degrades to reports and the scope must say so before building.",
                       "THE WRITE MECHANISM, RESHAPED. The GM emits journal / prediction / trigger entries inside its return contract as a machine-parseable block; a small umpire-side lander under src/ootp_ai/ validates the envelope and appends. This keeps ADR 0017\u0027s pen-holding intact and makes append-only MECHANICALLY true rather than promised, because the sole writer is code that can only append. State plainly in 0023 that this is a partial concession on the request\u0027s enforcement-by-grant theme, and why: no path-level permission layer exists.",
                       "gm/ MEMORY ARTIFACTS with settled contracts. journal (grain: one row per entry per sim date; key: (sim_date, seq)); predictions (one row per claim; key: seq; GM authors {subject, claim, resolve_by}, umpires author {resolution, resolved_at} as a NEW appended row, never an edit); triggers (one row per trigger; key: seq; fields: due_sim_date, recurring flag, and on fire a mandatory disposition of act / defer-with-new-date / close-with-reason, landed as a new row); execution-log (one row per proposal; key: seq; what / proposed / disposition / reason). Update semantics: append-only for all four, mechanically enforced. Coverage per gm/README.md\u0027s existing career-vs-club table - the execution log is career, the rest are club. plan.md is the sole mutable artifact.",
                       "LEDGER DISPOSITION: close gm/ledger.jsonl and open gm/execution-log.jsonl with a fresh seq space at 1. Keep the file, keep seq 1, never edit it - 0021\u0027s discipline applied to GM memory. Reason: continuing the seq space puts two incompatible schemas in one merge=union file whose two documented consumers (.claude/agents/gm.md forced-read item 3, gm/README.md\u0027s schema table) both say \u0027read it to learn doctrine\u0027.",
                       "APPEND-ONLY ENFORCEMENT BY HASH CHAIN, not by git history. Verifiable offline in a pure pytest at fetch-depth: 1, needs no .github/ edit (which is in the builder\u0027s deny set, tests/test_agent_contract.py:80), and delivers the request\u0027s own word - tamper-evident - rather than merely append-shaped. Also decide the .gitattributes treatment: merge=union interleaves lines and would break a chain, so these files probably must NOT be union-merged and must accept conflicts.",
                       "CONTEXT ASSEMBLY AS A MODULE, not umpire prose. A module under src/ootp_ai/ that reads gm/ and returns the invocation context (charter, current plan, triggers due at this sim date, standing orders, an index of journal/decisions), invoked by a skill. Put in code because it carries the trigger round-trip acceptance criterion, and a procedure written in prose cannot be asserted. Note: this is the first programmatic consumer of gm/ - nothing today reads it except agents and two structural tests.",
                       "DOC REWRITES: FRONT_OFFICE.md (the four-constraint table\u0027s \u0027Pause time\u0027 row at :31, the entire \u0027The action economy\u0027 section :48-65, and \u0027What you are allowed to see\u0027 :67-91); .claude/agents/gm.md (frontmatter tools, forced-read list, return contract\u0027s period/proposed sections, and the four prohibitions at :118-126); gm/README.md (layout, the career-vs-club scope table, the new schemas, and the period definition at :81-121 which becomes vestigial).",
                       "EXPLICIT DISPOSITIONS for the pinned work, written into the scope rather than left implied: first-sight Phases 10-11 (the two reports and the catalog), gm-inbox, and news-subscription-dial. Recommendation with reasoning: let first-sight finish 10-13 first. Its Phase 10 is the commit \u0027the request exists for\u0027 (IMPLEMENTATION_PLAN.md:1050), it is the project\u0027s own vertical-slice rule (CLAUDE.md: \u0027a beautiful pipeline that has never set a lineup has failed at both of this project\u0027s goals\u0027), and it produces the one thing this request currently asserts without evidence - a measured baseline of what the GM can do under the report wall, against which \u0027scarcity is a confound\u0027 becomes testable rather than argued."
                   ],
    "enhancements":  [
                         {
                             "title":  "Generate a gm_view.data_dictionary view from the same declaration",
                             "rationale":  "The request notes that first-sight\u0027s Phase 11 catalog becomes the GM\u0027s data dictionary with its withheld section shrunk to an explanation. Emit it as a VIEW rather than a tracked Markdown file: grain sentence, key list, coverage statement and epistemic label per table, straight from Contracts. Then \u0027what can I see, and what is deliberately missing\u0027 is a query on the same connection as everything else, self-consistent by construction - a column absent from gm_view is absent from the dictionary because both come from column_disposition. It also removes a class of drift the tracked catalog would otherwise carry.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Per-invocation cost instrumentation in the execution log",
                             "rationale":  "The request retires the action economy on the argument that the agent\u0027s tirelessness is the independent variable rather than a confound. Without measurement that claim is unfalsifiable, and ADR 0013\u0027s one quantitative claim (\u0027roughly 200 staff engagements per season rather than an open-ended optimization budget\u0027) is being deleted with nothing in its place. One tokens field and one wall-clock field per execution-log row, written by the umpire-side lander that core scope already builds. The request lists this under \u0027not now / later\u0027 and asks scoping to decide - it should ride along, because retrofitting it means a season with no baseline.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Carry ADR 0019\u0027s refusal loop into the execution log as a first-class disposition",
                             "rationale":  "0019\u0027s pricing dies but its refusal loop is orthogonal to price and is one of the two things the request itself flags as worth keeping (Open Question 5). Make disposition=\u0027declined\u0027 require a non-empty reason, and have the context assembler serve prior declines back at invocation. This is the durable, non-conversational refusal 0019:94-96 demanded, and it costs a required field plus a filter in code the core scope already writes.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Live-schema conformance test against the declaration",
                             "rationale":  "A gamedata test that reads information_schema.columns for the gm_view schema and diffs it against column_disposition over load_contracts(). Catches the failure the offline emitter test cannot see: a view hand-created in MySQL outside the generator, or a stale view left behind when a column\u0027s category changed. This is the same reasoning that makes tests/test_grain_contracts.py compare prose to emitted key rather than trusting either alone.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Land resolved predictions into the warehouse and serve a calibration view",
                             "rationale":  "The request\u0027s stated payoff for append-only memory is that \u0027its April belief survives contact with its July results, which is what makes calibration measurable\u0027. Measurable means queryable. A gm_view.calibration over landed predictions would let the GM grade itself - and would make ADR 0015\u0027s multi-year employment grading legible to the agent being graded. Deferred because it needs a new writer from gm/ into MySQL, a new grain, and a decision about whether GM-authored rows belong in a warehouse whose every other table comes from the save.",
                             "cost":  "grows-build"
                         },
                         {
                             "title":  "Retire ADR 0017\u0027s two-staff cost table and reprice repo agents",
                             "rationale":  "0017\u0027s table (:65-69) says repo agents cost \u0027Actions only\u0027. With actions gone, that column is empty and the in-game/in-repo distinction it draws is the sensor-vs-processing split that 0022 is amending anyway. Folding the repricing into 0022 keeps the amendment coherent; leaving it produces a live ADR whose cost column names a retired currency, which is exactly the drift the supersession discipline exists to prevent.",
                             "cost":  "cheap"
                         }
                     ],
    "risks":  [
                  "The enforcement claim rests entirely on an unverified harness capability. There is no .mcp.json and no .claude/settings.json in this repo (verified by recursive listing) so there is zero local evidence that a project-scoped MCP server can be registered and that an agent\u0027s tools: frontmatter can name exactly one of its tools. If it cannot, the only remaining SQL path is a Bash grant, which is strictly worse than ADR 0017\u0027s status quo, and the whole read half degrades to \u0027better reports\u0027. This should be spiked before anything is written - it is a one-hour check that decides half the scope.",
                  "gm_view protects almost nothing today. Measured: 1 withheld column out of 96, and it is name_category, a name byte filed rating-true purely to do withhold-by-default duty. No rating has landed; reviews/spike-scouted-view.md confirms ratings are stored in scouting.dat and first-sight lands none of them. The machinery is prospective insurance, and if the ratings slice never ships, this was built to protect an empty set. Argue it on the strength of the ADR 0012 posture rather than on present-day withholding.",
                  "Retiring the action economy removes the only stated bound on GM token spend and on optimize-everything-every-week behaviour (ADR 0013\u0027s Context names all three failures it prevents). The request replaces it with nothing - execution is metered, deliberation is not. That is a deliberate choice consistent with the operator\u0027s hypothesis, but it should be recorded as an accepted cost in 0022\u0027s Costs section, not omitted, because tests/test_repo_structure.py::test_every_adr_records_its_cost will not catch a cost that is merely understated.",
                  "The branch is mid-flight self-contradictory. Superseding four ADRs while rewriting FRONT_OFFICE.md, gm.md and gm/README.md means tests/test_doc_links.py (which resolves every relative link in every tracked .md) and any new agent-contract guard will fire on intermediate commits. Sequence the doc rewrite and the guard edit into the same commit, the same discipline first-sight\u0027s Phase 11 step 8 imposes for test_required_docs_exist.",
                  "merge=union and a hash chain are incompatible. .gitattributes marks gm/ledger.jsonl merge=union so two branches both keep their appends; union merge interleaves and does not preserve ordering relative to either side, so prev_hash links will not resolve after a union merge of two divergent appends. The new files need a decided merge strategy, and \u0027accept conflicts\u0027 is probably the right answer - but it reverses a convention gm/README.md:61 documents as deliberate.",
                  "first-sight is a planned artifact at Phase 10 of 14 whose AC20 is built entirely around handing Markdown into a Read/Glob agent. Landing this request first invalidates a committed plan mid-execution and forces a re-plan of a 177KB artifact. Landing it second means Phase 12 writes ADR-0016-shaped standing orders and Phase 13 appends a ledger row in the retiring schema - both cheap to supersede, which is why \u0027after\u0027 is the cheaper ordering even though it looks like the slower one.",
                  "ADR 0004\u0027s dbt question is forced whether or not this scope answers it. Generating and running views from contracts/ is literally its option 2 (\u0027Keep MySQL, drop dbt - hand-rolled SQL plus a thin runner. Loses lineage, tests, and docs\u0027). Appending a note to 0004\u0027s Notes - the mechanism first-sight\u0027s Phase 12 step 4 already planned for the same collision - is the minimum; letting the views land unremarked is the quiet divergence CLAUDE.md forbids.",
                  "gm_view must be pinned to ONE warehouse schema, and .env currently swaps MYSQL_DATABASE between ootp and ootp_dev (.env.example, ops/mysql-bootstrap.sql:23-31, with the comment explaining the split is defence in depth against a SELECT that forgets save_id). A GM pointed at ootp_dev would be managing a different universe with nothing erroring - the exact failure the schema split was created to prevent, arriving through a new door.",
                  "The GM gaining a query tool weakens a real property of ADR 0017: the isolation of the experimental subject. Today the GM demonstrably cannot reach the answer key because it holds no tool that can. After this, it cannot reach it because a grant says so - which is stronger against accident and weaker against a misconfigured grant, since a single widened GRANT in ops/mysql-bootstrap.sql silently reopens everything. The gamedata access-denied test is the only thing standing there, and it does not run in CI."
              ],
    "open_questions":  [
                           "Can this harness register a project-scoped MCP server from a tracked .mcp.json, and can an agent\u0027s tools: frontmatter name a single tool from it while excluding everything else? Unverified - no MCP config exists anywhere in this repo. This is the single highest-value thing to settle before planning, because it decides whether the read half is enforcement or prose.",
                           "Does gm_view serve max(ingest_seq)-latest by default with history opt-in, or full history? Recommendation: latest by default with a per-table _history sibling, because first-sight IMPLEMENTATION_PLAN.md:105 already establishes \u0027reports resolve to max(ingest_seq) for the requested sim_date by default and state the seq they read on line one\u0027 and one convention beats two. Cost: it doubles the view count. Needs a call, not a default.",
                           "Which warehouse schema does gm_view read from - ootp or ootp_dev - and how does the GM\u0027s DSN stay pinned to it when MYSQL_DATABASE is the value that swaps? A second .env key is the obvious answer; whether the generator refuses to build views over the dev schema at all is the sharper one.",
                           "Is a mutable plan.md coherent inside an otherwise append-only memory? The request wants git history as the supersession chain and each revision to state why it changed, but nothing in CI can assert that a diff carries a reason. Options: accept it as prose, or make plan revisions append-only entries with the current plan derived - which is a materially different artifact.",
                           "Where does the seq allocator for the new JSONL files live, and does the CLAUDE.md lesson about MySQL\u0027s SELECT FOR UPDATE have a filesystem analogue? Two branches appending concurrently will both read the same max(seq). The primary-key equivalent here is the hash chain itself - a broken chain IS the collision detector - but that should be stated as the mechanism rather than discovered.",
                           "One ADR or two? I recommend two (0022 information model, 0023 memory model) and the request lists this as its own Open Question 6. The counter-argument is real: they amend the same rulebook in the same commit and a reader hitting one without the other gets half a decision. If they land as one, its Consequences section carries two independent failure modes, which is the shape 0013 had before 0018 and 0019 patched it.",
                           "Does the field map\u0027s pre-registered \u0027unclassified\u0027 category get added here or deferred? contracts/field_map.toml\u0027s name_category comment explicitly routes it to \u0027whoever revisits ADR 0012\u0027, and this request is that revisit while declaring 0012 out of scope. Deferring is defensible; doing it silently is not. If deferred, say so in 0022 so the next reader does not think the note was missed.",
                           "Does the umpire-side lander live in src/ootp_ai/ (testable, importable, mypy-checked) or inside a skill\u0027s SKILL.md (procedural, unassertable)? The trigger round-trip acceptance criterion pushes it into code; the fact that it is umpire procedure rather than pipeline pushes it toward a skill. It can be both - a module invoked by a skill - but the split has to be named or the acceptance criterion has nothing to run against.",
                           "How does a catalog distinguish \u0027withheld, deliberately\u0027 from \u0027not landed yet\u0027? The request raises this and it matters more once withheld = absent: bronze_player has no position column because parser/players.py could not decode the role byte exactly, and bronze has no ratings because scouting.dat is unparsed. Neither is a policy withhold, and a GM that reads absence as prohibition will stop asking for capabilities it could have."
                       ]
}
```

---

## 

```json
{
    "scoper":  "ambitious",
    "ok":  true,
    "fit":  {
                "verdict":  "reshape",
                "rationale":  "The mechanical half fits this repo almost suspiciously well, and the governance half is exactly what the repo\u0027s ADR discipline is built to absorb — but three grounded findings mean it cannot be built as written.\n\nWHY IT FITS. `src/ootp_ai/contracts/policy.py::column_disposition` already decides releasability per declared column and fails closed on an unrecognised category (`policy.py:108-112`). `warehouse/ddl.py` already emits schema FROM the declaration rather than restating it, and refuses a vocabulary it has fallen behind (`ddl.py:123-138`). `db.py` already carries an asymmetric-connection pattern that sets a read-only session and then READS THE SETTING BACK rather than trusting it (`db.py:73-87`). `ops/mysql-bootstrap.sql:50-65` is already the tracked, public home for database-scoped grants with no GRANT OPTION. A `gm_view` schema generated from `column_disposition` and served to a SELECT-only user is the same single-declaration pattern the warehouse already runs, pointed at a new consumer. Measured just now: `load_contracts()` yields 8 tables / 96 columns, of which `column_disposition` returns 94 renderable, 1 withheld (`bronze_name.name_category`) and 1 uncertain (`bronze_league_event.real_sim_date`) — so the generator has real, non-vacuous behaviour to test on day one.\n\nWHY IT MUST RESHAPE, three reasons.\n\n(1) THE HEADLINE CLAIM IS CURRENTLY VACUOUS. \u0027A true rating is physically unreachable: the withheld columns are absent from the schema\u0027 describes a wall around an empty room. `field_map.toml` declares 10 `rating-true` fields and ZERO `rating-scouted` fields, and all ten `rating-true` entries are unclassified byte spans (`tail_extra_byte`, `unclassified_after_birth_date/age/nation/height/experience`, `unclassified_before_masks`, `identity_to_status_spans`, `name_category`, `name_usage_pairs`) wearing the category only because CLAUDE.md\u0027s withhold-by-default corollary assigns it — `field_map.toml`\u0027s own comment on `name_category` says exactly that. No rating has landed; `docs/data-access.md:561-576` still labels the own-scout-view question `unconfirmed`, and `reviews/spike-scouted-view.md` resolves only the OSA half (`measured`), leaving the club\u0027s own read `inferred`. The grant is still worth building — far cheaper before the answer key arrives than retrofitted after — but the acceptance criterion must be a property of the GENERATOR (a synthetic `rating-true` column never appears in emitted view SQL), not a claim about today\u0027s rows.\n\n(2) \u0027ENFORCEMENT BY GRANT\u0027 CANNOT DELIVER FOR WRITES, and the repo already says so. `.claude/agents/README.md:104-113`: \u0027This harness has no path-level permission system. Agent frontmatter\u0027s `tools:` key gates *which tools* an agent holds — that part is real and enforced — but nothing gates *which paths* those tools may touch.\u0027 Open Question 2 therefore has a measured answer: a Write/Edit grant scoped to `gm/` paths does not exist. A raw Write grant to the GM is an unbounded write grant that could edit `FRONT_OFFICE.md`, `.claude/agents/gm.md`, or the tests guarding it — the exact failure `tests/test_agent_contract.py::test_deny_set_still_protects_the_guards` exists to prevent for the builder. The asymmetry is structural: a MySQL GRANT is genuine prevention; a path allowlist is prose. The write channel must be a TYPED APPEND TOOL that validates an envelope and can do nothing else, or stay umpire-landed.\n\n(3) THE REQUEST CREATES A NEW PUBLIC-DATA PATH NOBODY HAS NAMED. `gm/` is tracked and world-readable forever (ADR 0011 + 0006), while rendered game data is deliberately routed to a git-ignored root — `.env.example` on `OOTP_OUTPUT_ROOT`: \u0027MUST be git-ignored: a rendered roster is OOTP\u0027s player data and this repo is public (ADR 0006)\u0027, asserted as first-sight AC14. Giving the GM a query tool AND a write channel into `gm/` reopens exactly the path that routing was built to close. `tests/test_no_leaks.py` covers drive paths, home directories and email addresses — not roster rows.\n\nOne more thing to price before planning: the GM\u0027s DSN cannot be the application DSN. `ops/mysql-bootstrap.sql:57-63` grants `ootp_ai` ALL PRIVILEGES on `ootp`, `ootp_dev` AND `ootp_truth_real` — the ground-truth export, which `reviews/spike-scouted-view.md` confirms carries true ratings. A separate `gm_reader` user is not a nicety; without it the GM reaches the answer key simply by connecting.\n\nADR 0005 placement: `gm_view` serves snapshot facts, so it sits on the parser+dbt side of the split — but dbt is deferred (first-sight Phase 12 step 4 records that deferral against ADR 0004 §Notes), so this ships as generated views and the scope should say so rather than letting a third serving mechanism appear unannounced. The `gm/` memory artifacts are governed by ADR 0011, not 0005: they cannot be rebuilt from the save, so they are tracked and are neither a builder dataset nor a dbt model; a derived read-model over them belongs in `var/`, which 0011\u0027s Forecloses section already settles."
            },
    "goals":  [
                  "Replace the report wall with a SCHEMA BOUNDARY: a `gm_view` schema whose views are generated from `contracts/policy.py::column_disposition` over the eight declared tables, so a withheld column is absent from the GM\u0027s schema rather than forbidden by prose — the same single-declaration pattern `warehouse/ddl.py` already uses to emit DDL.",
                  "Make ADR 0012\u0027s withhold-by-default posture STRONGER in the handover, not weaker: the same function that gates a report page gates the view\u0027s SELECT list, and an unrecognised category still fails closed (`policy.py:108-112`).",
                  "Create a `gm_reader`@localhost MySQL user with SELECT on `gm_view.*` and nothing else, in `ops/mysql-bootstrap.sql` beside the existing application grant — so that even if every prose rule failed, the connection cannot reach `ootp`, `ootp_dev`, or `ootp_truth_real` (which holds true ratings).",
                  "Give the GM a durable, append-only memory it writes itself — a journal, predictions, a dated trigger queue, an execution log — so an April belief survives contact with July results and calibration becomes measurable rather than asserted.",
                  "Give the GM a TIME HORIZON: a note written at sim date N is served back at sim date N+k with nobody remembering to do it, and a revisable `plan.md` whose git history is the supersession chain.",
                  "Retire the action economy and its two pricing patches, replacing budget adjudication with an EXECUTION LOG (proposed / executed / deferred / declined, with reasons) — metering execution rather than attention, which is what the operator\u0027s actual hypothesis requires.",
                  "Preserve *declare before doing* through the transition: reasoning is written before outcomes are known, enforced by append-only-ness rather than by the ledger\u0027s retired cost vocabulary.",
                  "Keep the ADR career record honest: supersede and amend, never delete, with `docs/decisions/README.md`\u0027s status table kept in step and asserted by a test.",
                  "Close the enforcement gap on the GM agent itself. Measured: `tests/` contains ZERO references to `.claude/agents/gm.md`, so nothing pins its tool grant — the follow-up first-sight Phase 13 step 4 already names.",
                  "Give the pinned work explicit dispositions rather than leaving it stranded: first-sight Phases 10–11, `gm-inbox`, and `news-subscription-dial` are all shaped or priced in vocabulary this request retires."
              ],
    "non_goals":  [
                      "Any change to ADR 0012 (scouted ratings only), 0015 (owner judges), 0003 (Challenge Mode), 0001 (no write-back), 0021 (append-only bronze), or 0006 (public repo). 0012\u0027s enforcement gets stronger; its content is untouched.",
                      "Widening what the parser reads. No new `.dat` file, no new field, no change to the snapshot file set. `gm-inbox` stays its own request.",
                      "Landing ratings of any kind. The `scouting.dat` slice — the one that would make the schema boundary load-bearing rather than prospective — is a separate request; this one builds the wall in advance.",
                      "Reconstructing true ratings from observables. ADR 0014\u0027s surviving core stands: analytics over scouted inputs and realized outcomes is in; regression toward the answer key is out.",
                      "A condition-evaluating trigger engine. Dated triggers only; SQL-condition triggers evaluated at ingest are explicitly deferred.",
                      "Building analytics, models, or reports FOR the GM. Directing that becomes the GM\u0027s job once this lands; pre-building it would pre-empt the experiment.",
                      "Introducing dbt. ADR 0004\u0027s deferral (recorded in first-sight Phase 12 step 4) stands; `gm_view` ships as generated views, and a later slice may re-home them as dbt models.",
                      "Silver or gold medallion layers. `gm_view` is a disposition-filtered projection of bronze, not a conformed model.",
                      "A general shell or a general database tool for the GM. The query channel is single-purpose or it is not a boundary.",
                      "A general Write/Edit grant for the GM. The harness cannot scope one by path (`.claude/agents/README.md:104-113`), so an unbounded grant would let the GM edit the guards that constrain it.",
                      "A warehouse retention policy. Storage still grows monotonically (ADR 0021\u0027s stated cost, driven by `bronze_name`\u0027s 264,095 rows per save per snapshot) and nothing here bounds it.",
                      "Writing the GM\u0027s baseball content — charter, competitive window, plan v1. The apparatus lands empty and the GM fills it.",
                      "Any change to in-game staff mechanics. Hiring a better scout remains the only route to a clearer sensor.",
                      "Fixing or widening `tests/test_no_leaks.py`\u0027s credential coverage in general — the `secret-scanning` request owns that. This scope adds only the pattern its own new `.env` keys require."
                  ],
    "acceptance_criteria":  [
                                "`uv run pytest tests/test_gm_view_emitter.py -m \u0027not gamedata\u0027` is green offline with no MySQL: fed SYNTHETIC contracts containing (a) a `rating-true` column, (b) a `rating-scouted` column labelled `unconfirmed`, (c) a column whose category is absent from `KNOWN_CATEGORIES`, and (d) a proven `identity` column, the emitted CREATE OR REPLACE VIEW text contains the name of (d) and contains NONE of (a), (b), (c) — asserted per column BY NAME with mismatches enumerated, never as a count.",
                                "Offline, over the real shipped declaration: for all 8 declared tables and all 96 declared columns, the emitted view\u0027s SELECT list equals exactly the set where `column_disposition(contracts, table, column) is Disposition.RENDERABLE`. The test names today\u0027s two exceptions explicitly — `bronze_name.name_category` (withheld) and `bronze_league_event.real_sim_date` (uncertain) — so a future change to either disposition turns the test red rather than silently widening the GM\u0027s schema.",
                                "`uv run pytest tests/test_gm_grant.py -m gamedata` is green locally: connecting as the GM user, SELECT succeeds against every emitted view, and EACH of the following raises an access-denied error (MySQL 1044/1142), enumerated per statement by name — `SELECT 1 FROM ootp.bronze_player`, `SELECT 1 FROM ootp_dev.bronze_player`, `SELECT 1 FROM ootp_truth_real.players_scouted_ratings`, `SELECT 1 FROM mysql.user`, an INSERT into a gm_view view, an UPDATE, a DELETE, and a CREATE TABLE in `gm_view`.",
                                "`-m gamedata`: querying `information_schema.columns` as the GM user returns ZERO rows for `table_schema IN (\u0027ootp\u0027,\u0027ootp_dev\u0027,\u0027ootp_truth_real\u0027)` — the withheld tables are not merely unreadable, they are not nameable from that connection.",
                                "`-m gamedata`: `SELECT @@session.transaction_read_only` on the GM connection returns 1, read back and asserted rather than assumed — the same proof `db.py::_assert_read_only` (`:73-87`) already performs for the ground-truth session.",
                                "Offline: a test parses `.claude/agents/gm.md`\u0027s YAML frontmatter and asserts its `tools:` value equals the exact decided list, string for string, with `Bash`, `Write`, `Edit`, `Task` and any general SQL tool asserted ABSENT by name. Today no test in `tests/` references `gm.md` at all (measured), so this also closes the follow-up first-sight Phase 13 step 4 named.",
                                "Offline: for every append-only artifact listed in `gm/README.md`\u0027s layout table, `.gitattributes` carries a matching `merge=union` line — derived FROM the README table, so adding an artifact without the attribute goes red. Today only `gm/ledger.jsonl` carries one.",
                                "`uv run pytest tests/test_gm_append_only.py` is green AND is proven to fire: for each protected `gm/` file the diff against the merge-base contains zero removed content lines, and a fixture that stages a deletion inside a throwaway temporary repository makes the same assertion fail — the negative case living in the same test file.",
                                "Trigger round-trip, offline, no MySQL, against a temporary `gm/` root: a trigger written at sim date N with `fire_on = N+k` is NOT served by the context assembler at N+k-1, IS served at N+k, and IS still served at N+k+1 while its disposition is unrecorded. All three assertions in one test.",
                                "Offline: the context assembler exits non-zero, naming the trigger id, when a fired trigger has no disposition recorded — so \u0027mandatory disposition on fire\u0027 is mechanical rather than requested.",
                                "Offline: every line of every `gm/*.jsonl` artifact validates against its declared envelope — an unknown key, a missing required key, and a wrong scalar type each fail, reported per file per line number rather than as one boolean.",
                                "Offline: each `gm/` JSONL artifact\u0027s declared key is unique across the file, AND the prose grain sentence in `gm/README.md` resolves to that same key list — the prose-versus-key agreement `contracts/loader.py` already enforces for bronze, applied to GM memory.",
                                "Offline: ADRs 0013, 0016, 0018 and 0019 each still exist on disk, each carries a `**Status:** Superseded by \u003cn\u003e` line, and the corresponding row in `docs/decisions/README.md` says the same thing — asserted by parsing both, so a supersession recorded in one place and not the other is red.",
                                "`uv run pytest -m \u0027not gamedata\u0027` passes with no game install and no MySQL; `ruff check .`, `ruff format --check .` and `mypy` are clean; `tests/test_no_leaks.py`, `tests/test_repo_structure.py`, `tests/test_doc_links.py` and `tests/test_agent_contract.py` are all still green.",
                                "`tests/test_no_leaks.py` gains a credential-shaped pattern covering the new GM `.env` keys, with a positive sample added to `test_patterns_still_catch_real_leaks` proving the guard catches it and a negative sample proving it does not fire on the repo\u0027s own prose. `.env.example` gains the new keys with empty values.",
                                "**USER-RUN.** A cold session spawns the `gm` subagent with only the assembled weekly context. It answers a named baseball question by issuing at least one query itself, and its returned handoff cites the `(save_id, sim_date, ingest_seq)` triple its answer was read from, with no roster fact appearing under `## assumed`.",
                                "**USER-RUN.** The operator confirms by hand that `OOTP-AI.lg`\u0027s file set, sizes and modification times are unchanged after a full GM invocation cycle — the ADR 0001 check that must not be performed by the code it audits."
                            ],
    "core_scope":  [
                       "THE ADR SET. Two new ADRs, because the two decisions are separable and have separate reasoning: (i) the information model — retire the action economy and the report wall, open a scoped schema, keep 0019\u0027s feed-vs-warehouse insight and its refusal loop; (ii) the memory model — append-only GM writes, the execution log, the revisable plan. Both supersede 0013/0016/0018/0019 as intake names; 0014 is amended to sensor-vs-processing; 0017 keeps the subagent while its no-DB and no-write foreclosures become scoped grants. Supersede, never delete; `docs/decisions/README.md`\u0027s table updated in the same commit.",
                       "`gm_view` GENERATION. A view emitter that turns `(Contracts, column_disposition)` into `CREATE OR REPLACE VIEW gm_view.\u003ctable\u003e` selecting exactly the RENDERABLE columns — withheld and uncertain columns ABSENT from the SELECT list, never nulled or renamed. Every identifier through `warehouse/sql.py::quote_ident`, exactly as `ddl.py` does. Placement decision required: if it lands under `src/ootp_ai/warehouse/`, note that ADR 0021\u0027s mutation scan bans `DROP\\s+(TABLE|SCHEMA|DATABASE|INDEX|COLUMN)` (`tests/test_bronze_landing.py:763`) and `DROP VIEW` slips through by omission — either extend the alternation or house the emitter in a sibling package and say why.",
                       "THE UNCERTAIN DISPOSITION IN A VIEW — a contract the request does not settle and cannot skip. `Disposition.UNCERTAIN` exists because a report page can carry a banner (`policy.py::render_with_uncertainty`, `uncertainty_banner`); a SQL view has no banner channel. Measured, exactly one landed column is affected today (`bronze_league_event.real_sim_date`). Decide and declare one of: exclude from `gm_view`; include with a `__unconfirmed` name suffix so the caveat travels with every result set; or include and carry the caveat only in the served data dictionary. Whichever is chosen, the emitter enforces it and a test pins it.",
                       "THE GRANT. A `gm_reader`@localhost user in `ops/mysql-bootstrap.sql`, SELECT on `gm_view.*` only, no GRANT OPTION, localhost-bound like the existing application user, with explicit absence of privilege on `ootp`, `ootp_dev`, `ootp_truth_real` and `mysql`. Its credentials in `.env` under new keys documented in `.env.example`; resolved by `config.py` (the only module that touches the environment) and reached by exactly one factory — `db.connect_gm` — which asserts a read-only session by reading `@@session.transaction_read_only` back, mirroring `db.py:73-87`.",
                       "THE QUERY CHANNEL, and what stops the GM reading `.env`. Decide between a single-purpose entry point reached through a narrowly-permitted Bash rule and a purpose-built tool named in the agent\u0027s `tools:` frontmatter. The frontmatter grant is genuine harness enforcement; a Bash permission rule is a separate layer for which this repo tracks no configuration file today. The GRANT is the floor either way, and the scope must state that the design assumes the channel could leak and the database still holds.",
                       "`gm/` MEMORY ARTIFACTS, each with a settled contract. `journal.jsonl` (grain: one entry per seq; envelope: sim_date, kind, subject, optional trigger, prose body; author: GM). `predictions.jsonl` (grain: one prediction per seq; subject, claim, resolve_by, resolution; GM authors the claim, umpires author the resolution). `triggers.jsonl` (grain: one trigger per seq; dated only; recurring-vs-one-shot explicit at write time; mandatory disposition on fire — act / defer with new date / close with reason). `execution-log.jsonl` (grain: one proposal per seq; what / proposed / disposition / reason). `plan.md` — mutable, git history is the supersession chain, each revision states why it changed. Every file: append-only, `merge=union` in `.gitattributes`, career-vs-club scope recorded in `gm/README.md`\u0027s existing table, and a validated envelope.",
                       "THE APPEND-ONLY CI GUARD, with the `merge=union` interaction worked out. Union merge keeps both sides\u0027 lines, which helps for appends; the guard\u0027s job is to catch a REWRITE. A diff-against-merge-base check with zero removed content lines is the shape, and the guard must itself be proven to fire against a staged deletion — this repo\u0027s standing rule is that an unobserved guard is decoration (first-sight §4.4).",
                       "LEDGER DISPOSITION. Recommended: CLOSE `ledger.jsonl` with a terminal marker rather than migrate it. Its `proposed`/`ruling` vocabulary (`cost`/`free`) has no meaning under the new model, and rewriting a tracked append-only file to fit a new schema is precisely the retroactive edit this request exists to make impossible. Open `execution-log.jsonl` beside it with its own seq space; `gm/README.md` records that the ledger is historical and why. Seq 1\u0027s actual reasoning — *choosing between options the owner has already presented is free* — survives as doctrine even though its currency does not.",
                       "THE TRIGGER SERVICE AND CONTEXT ASSEMBLY. Where the weekly invocation-context builder lives (a skill, a module under `src/ootp_ai/`, or umpire procedure) and what it includes by default: charter + current plan + fired triggers + standing refusals + standing orders + a one-line index of journal and decisions with pull-on-demand rather than full history. It must resolve the current sim date from the warehouse or the ingest run rather than wall-clock, so the trigger clock is the league\u0027s clock — the same reasoning that keeps `sim_date` out of wall-clock in every bronze key.",
                       "THE AGENT DEFINITION AND ITS GUARD. `.claude/agents/gm.md` rewritten: the tool grant, the forced-read list (which currently sends the GM to `ledger.jsonl` to learn doctrine — a file about to be closed), the return contract (currently built around `proposed: cost|free` with a `precedent` seq per action), and every prohibition citing a superseded ADR. `tests/test_agent_contract.py` extended to cover `gm.md`; today it is scoped to `data-engineer.md` alone (`:18`).",
                       "`FRONT_OFFICE.md` REWRITTEN. Its four-constraint table names ADR 0013 as \u0027Pause time\u0027 — that row changes or goes. The `## The action economy` section and the `## What you are allowed to see` section are rewritten around the schema boundary, and the three surviving constraints (0003, 0012, 0015) are restated as the load-bearing set.",
                       "PINNED-WORK DISPOSITIONS, written down rather than inferred. `first-sight` Phases 10–11: recommend first-sight ships its reports and catalog FIRST and this request converts them to seed views afterwards — Phase 10 is one commit away and is \u0027the commit the request exists for\u0027, and Phase 11 wires `docs/warehouse-catalog.md` into the required-docs guard in the same commit as its generator. `gm-inbox`: re-frame — its ADR 0018 tier-2 pricing argument evaporates and it becomes a plain parser-widening slice. `news-subscription-dial`: its central gap (the ledger has no vocabulary for refusal) is solved for free by the execution log\u0027s `declined` disposition, so it shrinks to the twelve-category inventory plus the subscription-state record.",
                       "THE PUBLIC-DATA DECISION. Explicitly settle whether OOTP player data may appear in tracked `gm/` files now that the GM both queries and writes. `.env.example` and first-sight AC14 route rendered rosters to a git-ignored root precisely because the repo is public; a journal entry pasting a query result reopens that path, and `tests/test_no_leaks.py`\u0027s three patterns would not see it. Either bound what may be quoted (an entry cites a query and a triple, not a result set) or accept it in writing with ADR 0006\u0027s reasoning addressed."
                   ],
    "enhancements":  [
                         {
                             "title":  "A `gm_view.withheld_columns` view — serve the negative space, never the values",
                             "rationale":  "The GM should price a gap rather than discover it by hitting one. A view listing table, column, reason and governing ADR for every column the disposition withholds gives the GM the shape of its blindness without a single withheld value crossing the boundary. It is generated from the same `column_disposition` pass that builds the views, so it cannot drift from what was actually excluded — and it makes intake\u0027s own line (\u0027withheld = absent from schema, so the catalog\u0027s withheld section shrinks to an explanation\u0027) true by construction rather than by editing.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A `gm_view.data_dictionary` view served from the already-landed `bronze_field_label`",
                             "rationale":  "`bronze_field_label` already lands one row per landed column per snapshot carrying `category`, `epistemic`, `validator` and `source_file` — 96 columns across all eight tables, with the label AS OF THE DAY IT LANDED. Exposing it turns \u0027how sure are you about this column\u0027 into a question the GM can ask itself, so epistemic discipline is enforced at the point of use rather than by remembering a doc. Pure reuse: the table exists, is landed, and needs only a view.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Latest-resolving views alongside `_history` views, answering Open Question 9",
                             "rationale":  "ADR 0021 states plainly that \u0027a query that omits `ingest_seq` reads an ambiguous grain the moment any date is ingested twice\u0027 and calls it a real ergonomic tax. Serve both: `gm_view.\u003ctable\u003e` resolving `max(ingest_seq)` at the latest `sim_date`, and `gm_view.\u003ctable\u003e_history` carrying every snapshot. Correct-by-default ergonomics, with the right to look back that 0018\u0027s retention reasoning already grants. Every view still exposes the triple so a journal citation stays verifiable.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Pin every GM view to the managed `save_id`",
                             "rationale":  "`.env.example` warns in its own words that `save_id` is a column, so \u0027a SELECT that forgets it mixes two universes silently\u0027 — and the repo already pays for a separate `ootp_dev` schema as defence in depth against exactly that. The GM has no business seeing the probe or the Challenge-mode twin. A `WHERE save_id = \u003cmanaged\u003e` baked into the generated view makes the mistake unreachable rather than merely discouraged, and costs one config value.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A worked query cookbook in the GM\u0027s forced-read list",
                             "rationale":  "A cold, per-invocation agent writing SQL from scratch against an append-only snapshot warehouse will write the ambiguous-grain query ADR 0021 warns about. Three or four worked examples — resolve the latest seq, diff two seqs of one sim date, join a roster across the cross-team fan-out that `list_id` produces (measured: 935 players sit on more than one club\u0027s lists) — cost nothing and prevent the likeliest category of silently-wrong GM reasoning. Complements the latest-resolving views: those make the common case safe, this makes the uncommon one correct.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Session guardrails on the GM connection: read-only session, statement timeout, and a row cap",
                             "rationale":  "`bronze_name` lands 264,095 rows per save per snapshot — ADR 0021 records that as its largest known cost. An unbounded `SELECT *` would flood the GM\u0027s context and produce a worse decision than no query at all. `MAX_STATEMENT_TIME`, an enforced row cap, and the read-back read-only assertion make the connection safe against the GM\u0027s own enthusiasm, and the read-only session is belt-and-braces under the grant — exactly the posture `db.py` already takes for the ground-truth database.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A query provenance log — the replacement denominator for the retired action economy",
                             "rationale":  "ADR 0013\u0027s genuinely load-bearing property was not the budget but the DENOMINATOR it created: \u0027twelve actions on the draft and three busted picks is a return on invested attention\u0027, which ADR 0012\u0027s Buys section leans on to make scout quality assessable. Retiring the economy discards that unless something replaces it. Logging every GM query (statement, sim date, row count, invocation id) append-only gives a measured record of where the front office spent its attention — strictly better evidence than a budget, because it is observed rather than rationed, and it makes the operator\u0027s stated hypothesis measurable: tirelessness becomes an instrumented variable instead of a metered one.",
                             "cost":  "grows-build"
                         },
                         {
                             "title":  "Per-invocation token and turn instrumentation, riding along",
                             "rationale":  "Intake flags this as \u0027cheap and worth doing early\u0027 and leaves the sequencing to scoping. It pairs naturally with the query log: together they answer \u0027what does one week of this front office cost, and is it converging?\u0027 — and retiring ADR 0013 removes the only cost bound the project had (0013\u0027s Buys claimed \u0027roughly 200 staff engagements per season rather than an open-ended optimization budget\u0027). Filing it separately means building the same invocation-record plumbing twice.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A prediction resolver and a calibration report",
                             "rationale":  "Predictions without a resolver rot into a write-only file. An umpire-run resolver that walks `predictions.jsonl` for entries whose `resolve_by` has passed, prompts for an outcome and appends a resolution — plus a scoring pass (hit rate and a Brier-style score, sliced by subject class) — converts \u0027the reasoning record is tamper-evident\u0027 into \u0027the GM\u0027s calibration is measurable\u0027, which is the payoff the request names. It also gives ADR 0015 a second instrument: an owner can fire a well-calibrated but unlucky GM, and the record would show it.",
                             "cost":  "grows-build"
                         },
                         {
                             "title":  "Standing refusals served back at every invocation",
                             "rationale":  "ADR 0019\u0027s refusal loop is one of the two things intake says should survive the pricing, and its three stated properties (the reason travels, refusing costs nothing, it is durable rather than conversational) map exactly onto a `declined` disposition in the execution log plus a section in the assembled context. Zero new machinery — and it retires the central gap of the `news-subscription-dial` request as a side effect, which is genuine cross-request reuse rather than a claimed one.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A `/gm-week` skill that runs the whole cycle end to end",
                             "rationale":  "The request describes a weekly cycle — ingest, assemble context, spawn, capture, log — with no owner. A skill that assembles the context, spawns the GM, validates the returned envelope against the declared schemas, appends the journal and execution-log rows, and stops for the operator\u0027s dispositions is the difference between an apparatus that exists and one that gets run. It also becomes the single place the append-only discipline is applied, which is far safer than trusting each invocation to append correctly.",
                             "cost":  "grows-build"
                         },
                         {
                             "title":  "Mirror the GM\u0027s own memory into `gm_view` as queryable views",
                             "rationale":  "Once the journal, predictions and execution log carry declared envelopes, the GM can query its own history instead of reading whole JSONL files into a context window that shrinks every season. \u0027What did I say about this player before?\u0027 becomes a query, and calibration analysis becomes SQL rather than prose. Placement is already settled by precedent: the JSONL files stay the tracked source of truth in `gm/` per ADR 0011, and the mirror is a derived read-model, which 0011\u0027s Forecloses section assigns to `var/`.",
                             "cost":  "grows-build"
                         },
                         {
                             "title":  "A decision-replay harness — the \u0027drift ritual\u0027, brought forward",
                             "rationale":  "Intake defers this to \u0027not now\u0027, but the context assembler makes it nearly free: replay a past invocation\u0027s exact assembled context against the current agent definition and diff the decisions. It answers the question a rules refactor of this size actually raises — did changing the rulebook change the GM, and where? Listed as an enhancement rather than core because it has no value until several invocations exist.",
                             "cost":  "grows-build"
                         },
                         {
                             "title":  "Emit the GRANT statements from the same declaration that emits the views",
                             "rationale":  "Two statements of one fact drift — the reasoning `tables.toml`\u0027s own preamble gives for why the DDL is emitted rather than written. A hand-maintained grant list beside a generated view set is exactly that shape: add a view, forget the grant, and the boundary is wrong in the invisible direction or the over-granted one. Emitting `GRANT SELECT ON gm_view.\u003cview\u003e` per generated view, and asserting the applied grants match the emitted set, keeps the pattern intact and makes over-granting a test failure.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Extend the ADR 0021 mutation scan to cover `DROP VIEW`, or house the emitter outside `warehouse/`",
                             "rationale":  "Measured: the banned pattern at `tests/test_bronze_landing.py:763` is `DROP\\s+(TABLE|SCHEMA|DATABASE|INDEX|COLUMN)` — `DROP VIEW` passes by omission. A view emitter that drops and recreates would sail past a guard whose entire purpose is that a destructive statement in `warehouse/` becomes a decision rather than a diff. Widen the alternation with a named exemption, or put the emitter in a sibling package and record why. Cheap now, considerably less pleasant to discover later.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A byte-deterministic committed snapshot of the emitted view SQL, asserted in CI",
                             "rationale":  "The catalog already earns its honesty this way — first-sight Phase 11 commits the structural half and asserts byte-identity so it cannot be hand-edited into drift. The same trick on the emitted `gm_view` DDL means any widening of the GM\u0027s schema appears as a reviewable diff in a tracked file, visible to a human at `/commit`, and turns \u0027nobody quietly added a column to the GM\u0027s view\u0027 from a hope into a CI assertion. It needs neither a save nor a database, so it runs offline with real CI signal.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A dated \u0027what changed\u0027 migration note for the GM\u0027s first post-refactor invocation",
                             "rationale":  "The GM is a per-invocation subagent with no memory of the transition. The first invocation after this lands reads a rewritten `FRONT_OFFICE.md` and a closed ledger with no way to know its predecessor operated under different rules — which makes its `## situation` section subtly wrong about its own history, every time, until someone notices. A tracked note in `gm/` stating what was retired, what survives, and where seq 1\u0027s reasoning went costs an hour.",
                             "cost":  "cheap"
                         }
                     ],
    "risks":  [
                  "THE WALL GUARDS AN EMPTY ROOM TODAY, and a reader of the ADR will not know it. Measured: zero `rating-scouted` fields are declared, no rating column has landed, and all 10 `rating-true` entries in `field_map.toml` are unclassified byte spans holding the category by withhold-by-default. An ADR claiming the GM \u0027physically cannot reach a true rating\u0027 without stating that no true rating is in the warehouse is technically true, rhetorically misleading, and will be cited later as though it had been tested.",
                  "THE GRANT IS ONLY AS GOOD AS THE CONNECTION THE TOOL ACTUALLY OPENS. The `gm_reader` DSN and the `ootp_ai` DSN both live in `.env`, the GM holds `Read`, and the harness has no path-level permissions — so nothing structurally stops the GM reading the application credentials. Whether that is exploitable depends entirely on the query channel\u0027s design: a Bash rule leaves it live, a purpose-built tool that never accepts a DSN closes it. The scope must not describe the boundary as airtight without settling this.",
                  "THE WRITE CHANNEL IS THE WEAKEST LINK AND IT IS THE REQUEST\u0027S CENTRAL THEME. \u0027Enforcement by grant rather than prose\u0027 is achievable for reads (MySQL) and NOT for writes (`.claude/agents/README.md:104-113`). A Write grant to close the loop would hand the GM the ability to edit `FRONT_OFFICE.md`, `.claude/agents/gm.md`, and the very tests pinning its tool grant — strictly worse than 0017\u0027s no-write foreclosure. The typed-append-tool mitigation is sound but adds a component the request does not budget for.",
                  "TRACKED, PUBLIC GM MEMORY MEETS A QUERY TOOL. `gm/` is world-readable forever; rendered game data is deliberately git-ignored. A GM that can query and write can paste OOTP player rows into a tracked file, and `tests/test_no_leaks.py` will not catch it — its patterns cover drive paths, home directories and email addresses, not roster data. This is a new path the request creates and does not name.",
                  "REMOVING SCARCITY REMOVES THE DENOMINATOR. ADR 0013\u0027s genuinely load-bearing property was the measurement scarcity enabled, which ADR 0012\u0027s Buys section leans on to make scout quality assessable and ADR 0018 extends across seasons. Retiring the economy without instrumenting query and invocation cost leaves ADR 0014\u0027s \u0027was the scouting director worth it\u0027 with no denominator at all. The query-provenance enhancement is the mitigation and probably should not be optional.",
                  "SUPERSEDING FOUR ADRS AT ONCE RISKS DROPPING A LOAD-BEARING CLAUSE BY ACCIDENT. 0018 contains the foresight-trap argument (retention is irreversible, so charging for it penalises prophecy rather than judgement) and 0019 contains the anti-laundering counterfactual and the feed-versus-warehouse asymmetry. Both retain force under the new model even though their pricing does not. A supersession that does not explicitly re-home them loses reasoning that took two ADRs to reach.",
                  "SCHEMA-BOUNDARY ENFORCEMENT IS ONLY AS HONEST AS THE FIELD MAP. The boundary is `column_disposition` over a hand-maintained declaration. A field mislabelled `identity` when it is a rating is released with no error surfaced — precisely the correctness trap CLAUDE.md names as the most dangerous in the project. `WITHHELD_NAME_FRAGMENTS` is the secondary backstop and matches exactly three fragments (`prone_`, `players_value`, `_talent_`). The wall moves the enforcement point; it does not improve the classification, and the ADR must not imply otherwise.",
                  "FIRST-SIGHT IS PLANNED AND MID-FLIGHT. Phase 10 is next; Phase 11 adds `docs/warehouse-catalog.md` to the required-docs guard in the same commit as its generator (and its own note says adding the entry a commit early turns CI red); Phase 13 step 3 requires an umpire ledger row about report free-ness whose vocabulary this request retires. Landing this first means amending a plan whose phases are sequenced against interlocking acceptance gates. Landing it second costs a conversion pass. Either way the interaction is real and cannot be deferred to implementation.",
                  "THE `merge=union` INTERACTION IS SUBTLER THAN THE APPEND-ONLY GUARD MAKES IT LOOK. Union merge keeps both sides\u0027 lines, which is right for appends and wrong for a file where a monotonic `seq` encodes order: two branches each appending `seq: 5` both survive, and the key-uniqueness assertion goes red at merge time rather than at write time. Either the seq allocation must tolerate that, or the seq must be allocated at land time by the assembler rather than by the GM.",
                  "THE OPERATOR IS STILL IN THE LOOP EVERY WEEK, JUST DIFFERENTLY. ADR 0013\u0027s stated cost was \u0027the operator is in the loop every period ... a standing tax on playing\u0027. The new model asks for execution dispositions, prediction resolutions and trigger dispositions — plausibly a heavier tax rather than a lighter one, and nothing in the request measures before-versus-after. The ledger currently holds exactly one entry; the new model asks for several artifacts per week.",
                  "A THIRD SERVING MECHANISM APPEARS WITHOUT AN ADR. The repo has builders + `datasets/` (ADR 0005), a dbt medallion (deferred), and now generated views. Nothing is wrong with that, but 0005\u0027s split is stated as a two-way rule and CLAUDE.md forbids speculative structure. Place `gm_view` on the parser+dbt side of 0005 explicitly and record it as an instance of the existing dbt deferral, rather than letting a third pattern arrive unnamed.",
                  "CONTEXT ASSEMBLY BECOMES THE NEW SINGLE POINT OF FAILURE. Once the GM\u0027s memory is several append-only files plus a plan plus a trigger queue, what reaches the invocation IS the GM\u0027s world. A bug in the assembler — a trigger silently not served, a plan revision missed, a refusal dropped — produces a GM reasoning confidently from an incomplete past with no error surfaced. That is the same failure shape as a parser reading the adjacent u16, and it deserves the same per-item enumerated testing rather than a smoke test.",
                  "THE GM\u0027S FIRST QUERIES WILL BE WRITTEN AGAINST A GRAIN IT HAS NEVER SEEN. The roster fan-out is cross-team (measured: 935 players on more than one club\u0027s lists), the 60-day IL drops a player from the 40-man so it cannot be derived, and roughly 10,700 of 18,072 players carry no roster row at all. A GM issuing its own SQL will get \u0027who is on Boston\u0027s roster\u0027 wrong in a way that reads as a confident answer. Documented coverage in the served dictionary plus the cookbook is the mitigation, and it is not optional."
              ],
    "open_questions":  [
                           "FIRST-SIGHT SEQUENCING. Recommended: first-sight ships Phases 10–13 as planned, then this request converts the two reports into `gm_view` seed views and reshapes the catalog into the served data dictionary. Phase 10 is one commit away and is \u0027the commit the request exists for\u0027; Phase 11 wires the catalog into the required-docs guard in the same commit as its generator. The operator should confirm — absorbing `gm_view` into first-sight as an amended phase is genuinely available and avoids building a report layer that is immediately converted.",
                           "GM WRITE MECHANISM, now that the harness answer is known. Since a path-scoped write grant does not exist, choose between (a) a typed append tool the GM holds in `tools:` that validates an envelope, appends, and can do nothing else, and (b) the GM returns structured output and the umpires land it, keeping 0017\u0027s pen-holding intact. A hybrid — the GM appends journal and prediction entries, the umpires author every resolution and disposition — is probably the right shape and should be decided here rather than in planning.",
                           "GM QUERY MECHANISM, and specifically what stops the GM reading `.env`. A purpose-built tool holding the DSN internally and accepting only a statement is the strong answer; a Bash permission rule is weaker and this repo tracks no settings file to write it into. State which, and state the residual exposure honestly rather than describing the boundary as complete.",
                           "LEDGER CONTINUITY. Recommended: close `ledger.jsonl` with a terminal note and open `execution-log.jsonl` beside it. Rewriting a tracked append-only file to fit a new schema is the exact retroactive edit this request is built to prevent. Confirm — continuing the seq space with a widened schema keeps precedent citation continuous and is defensible.",
                           "WHAT SURVIVES OF 0019. Its refusal loop maps cleanly onto a `declined` disposition plus a standing-refusals section in the assembled context. Its feed-versus-warehouse asymmetry (\u0027infrastructure can never replace what only the world knows\u0027) is a durable observation with no pricing attached and belongs in the new information-model ADR\u0027s Notes rather than in a superseded file. Confirm both land somewhere.",
                           "ONE ADR OR TWO. Recommended two: the information model and the memory model are separable decisions with separate reasoning and separate failure modes, and a single ADR superseding four while amending two is a document nobody can cite precisely later. Both would be written and accepted together.",
                           "APPEND-ONLY CI MECHANICS. The diff-against-merge-base shape is straightforward; the `merge=union` interaction with a monotonic `seq` is not. Two branches each appending `seq: 5` both survive a union merge and produce a duplicate key. Decide whether the guard runs pre-merge, whether the key becomes branch-independent, or whether the seq is allocated at land time by the assembler rather than by the GM.",
                           "CONTEXT ASSEMBLY OWNERSHIP. A skill is the natural home if the cycle is operator-driven; a module under `src/ootp_ai/` is the natural home if it is to be tested like pipeline code — which, given it becomes the GM\u0027s entire world, it should be. Recommended: a module carrying the tests, driven by a thin skill.",
                           "HISTORY EXPOSURE DEFAULT. Recommended: latest-resolving views by default plus explicit `_history` views, per ADR 0021\u0027s own warning that a query omitting `ingest_seq` reads an ambiguous grain. Retention is free under 0018\u0027s reasoning; this is purely about which mistake is easier to make.",
                           "THE UNCERTAIN DISPOSITION IN A VIEW — new, and absent from intake\u0027s list. `Disposition.UNCERTAIN` presumes a banner channel SQL does not have. Exactly one landed column is affected today (`bronze_league_event.real_sim_date`). Exclude it, suffix the column name so the caveat travels with every result set, or carry the caveat in the data dictionary only?",
                           "MAY OOTP PLAYER DATA APPEAR IN TRACKED `gm/` FILES? — new. The repo currently routes rendered rosters to a git-ignored root because it is public. A querying, writing GM reopens that path. Bound what may be quoted, or accept it explicitly against ADR 0006\u0027s reasoning.",
                           "ONE `gm_view` SCHEMA OR ONE PER UNIVERSE? — new. The warehouse already splits `ootp` from `ootp_dev` as defence in depth against a SELECT that forgets `save_id`. Does the GM\u0027s schema mirror that split, or is a `save_id` predicate baked into every view sufficient?",
                           "WHAT REPLACES THE CITATION-INTEGRITY PROPERTY THE REPORT FILES PROVIDED? — new. first-sight partitions reports by `(save_id, sim_date, ingest_seq)` specifically so \u0027the exact bytes the GM read are still on disk\u0027 months later. Under a query model the equivalent is the recorded triple plus append-only bronze — arguably stronger, because it is re-runnable rather than frozen. Say so in the ADR, or the property is lost by silence."
                       ]
}
```

---

## 

```json
{
    "scoper":  "minimalist",
    "ok":  true,
    "fit":  {
                "verdict":  "reshape",
                "rationale":  "The request fits the repo\u0027s grain — declaration-driven enforcement (contracts/policy.py, warehouse/ddl.py), append-only discipline (ADR 0021), supersede-never-delete ADR convention (docs/decisions/README.md:51) — and its motivation is a preference about which hypothesis is under test, which needs no empirical justification. But as written it bundles three separable things at wildly different risk: (a) retiring the action economy, which is pure governance, needs no new code, and is nearly free; (b) giving the GM durable append-only memory, which needs a schema and one CI guard; and (c) opening a granted `gm_view` schema to a GM-held SQL tool, which rests on a harness capability the repo has never exercised and which its own `.claude/agents/README.md:104-113` says does not exist in the form the request assumes. Three concrete reshapes follow. FIRST, the harness cannot scope a write grant by path — `.claude/agents/README.md:104` states plainly \u0027This harness has no path-level permission system... nothing gates *which paths* those tools may touch,\u0027 and :110-113 adds that the effective permission is the intersection with an invisible layer. So Open Question 2\u0027s option A (a `Write` grant scoped to `gm/` paths) is prose enforcement in a request whose thesis is that prose failed (ADR 0017 Context). Cut it: the GM emits structured entries and the umpire appends them, which keeps ADR 0017\u0027s pen-holding AND makes append-only mechanically true rather than promised. SECOND, the GM query mechanism is an unverified unknown, not a design detail. There is no `.claude/settings.json` in the repo, no MCP server, and no precedent for a subagent holding anything but `Read, Glob` and `data-engineer`\u0027s builder set. The only in-repo tool that can execute SQL is `Bash`, which would also hand the GM `.env`, `players.dat`, `ootp_truth_real` and the parser — obliterating every enforcement claim the request makes. This must be a gated spike, not an assumption. THIRD, several proposed artifacts violate the repo\u0027s own anti-speculation rule (`gm/README.md:51`: \u0027The directory does not split until a second club exists — don\u0027t build the tenure structure speculatively\u0027): predictions as a third file, `plan.md` as a fourth, recurring triggers, a context-assembly builder. Merge, defer or cut all four. Reshaped this way the cheap two-thirds ships immediately and the expensive third ships behind a proof."
            },
    "goals":  [
                  "Retire the attention-scarcity apparatus outright: supersede ADR 0013 and its two pricing patches (0018, 0019), so no future information channel needs its own pricing ruling. Adjudication overhead goes to zero because there is nothing left to adjudicate.",
                  "Replace the action ledger with an execution log whose only subject is what the operator actually did — proposed / executed / deferred / declined, each with a reason — preserving ADR 0019\u0027s refusal loop and ADR 0013\u0027s declare-before-doing, both of which survive the pricing they were attached to.",
                  "Give the GM a durable, tamper-evident memory it authors and can never revise: one append-only `gm/journal.jsonl` carrying dated triggers and dated claims, written by the umpire from the GM\u0027s structured return, so an April belief survives contact with a July result.",
                  "Close the loop mechanically: a trigger written at sim date N is surfaced at sim date N+k by a pure function over the journal, with no human remembering to look.",
                  "Move ADR 0012\u0027s withhold-by-default posture from the report page to a schema boundary — a `gm_view` schema generated from `contracts/policy.py::column_disposition` over the declared tables, served to a MySQL user holding `SELECT` on that schema and nothing else — so a withheld column is absent rather than forbidden.",
                  "Prove, before committing to the grant, that a `gm` subagent can hold a query tool that reaches the restricted schema and cannot reach `.env`\u0027s credentials, the writable warehouse, or `ootp_truth_real`. If it cannot, the grant half does not ship and the retirement half still does.",
                  "Pin the GM\u0027s tool grant in CI. Nothing under `tests/` asserts anything about `.claude/agents/gm.md` today (measured: zero grep hits across `tests/*.py`), which is the one guard that makes every other claim in this request checkable — and it is already a named follow-up in first-sight Phase 13 step 4."
              ],
    "non_goals":  [
                      "A `Write` or `Edit` grant for the GM, at any path scope. The harness cannot enforce path scoping (`.claude/agents/README.md:104`), so granting it converts an enforced constraint into a prose one inside a request whose whole argument is that prose failed. The umpire keeps the pen; the GM\u0027s return is its authorship.",
                      "A separate `gm/predictions.jsonl`. A prediction is a journal entry with `resolve_by` set and an umpire-authored resolution. One file, one `kind` discriminator. A second file buys a second schema, a second guard, and a second set of grain questions for zero capability.",
                      "A separate mutable `gm/plan.md`. `gm/charter.md` already exists, is club-scoped, is mutable, and its own header (`gm/charter.md:7-8`) already requires that changes be recorded as decision records with reasoning — that IS the revisable plan with a git supersession chain. It has been blocked as \u0027unwritten\u0027 since 2024-03-07 (`gm/charter.md:10-15`) precisely because the GM had no data. Write the charter; do not add a fourth artifact beside it.",
                      "Recurring triggers, or any trigger that evaluates a condition. Dated one-shots only. The mandatory disposition on fire (\u0027defer with a new date\u0027) already re-arms anything genuinely recurring, and a scheduler is a maintenance surface with zero current volume.",
                      "A context-assembly builder — a skill or a module under `src/ootp_ai/` that packages the weekly invocation context. `gm.md`\u0027s forced-read list plus `Glob` already does this, and trigger volume is currently zero. Building an assembler before there is anything to assemble is the definition of premature.",
                      "Rewriting or absorbing first-sight Phases 10–13. Ship them exactly as planned. Phase 10\u0027s two reports are ~2 files and one test; the apparatus being retired is the *commissioning economy*, not the rendering code, and Phases 12–13 carry the doc truth-up, the ADR 0001 read-only proof and AC20 — all needed under either information model.",
                      "Migrating `gm/ledger.jsonl` seq 1 into the new execution log. One row, recording a *free* ruling under a cost model being retired. `docs/decisions/README.md:51` says supersede rather than edit; apply the same to the ledger — close it with a note and open the execution log at seq 1.",
                      "Views over all eight declared tables, or a latest-vs-history opt-in mechanism. Four views, one shape. `bronze_name` alone is 264,095 rows per snapshot per save and holds the single withheld landed column; the GM has no business reading a raw index table.",
                      "Any change to ADR 0012, 0015, 0003, 0001, 0021, 0006 — as the request itself states. Also no widening of what the parser reads: no new file, no new field, no `scouting.dat` work.",
                      "A second data dictionary. Phase 11 of first-sight already generates a catalog with a withheld section carrying reason and ADR. Reuse it; a `gm_view`-specific dictionary is a duplicate that will drift."
                  ],
    "acceptance_criteria":  [
                                "`uv run pytest tests/test_agent_contract.py -k gm` is green: `.claude/agents/gm.md`\u0027s frontmatter `tools:` parses to exactly the declared set, and the assertion names `Bash`, `Write`, `Edit`, `Task` and `NotebookEdit` as forbidden members. Measured today: zero tests reference `gm.md`, so this is net-new coverage and also closes the follow-up named at first-sight IMPLEMENTATION_PLAN.md:1108.",
                                "`uv run pytest tests/test_gm_memory.py::test_the_append_only_chain_is_intact` is green offline: every line of `gm/journal.jsonl` and `gm/execution-log.jsonl` carries `seq` equal to the prior line\u0027s `seq` plus one and `prev` equal to the SHA-256 of the prior line\u0027s exact bytes, with line 1 carrying `prev: null`. Editing any historical character turns it red. No git, no network, no MySQL.",
                                "`uv run pytest tests/test_gm_memory.py::test_a_trigger_written_at_n_is_served_at_n_plus_k` is green: given a fixture journal holding a trigger with `resolve_by` = N+k, the due-trigger selector returns it when called at sim date N+k and returns nothing when called at N-1. A pure function over a file — the round-trip the request asks for, provable with no harness.",
                                "`uv run pytest tests/test_gm_memory.py::test_a_claim_is_never_self_resolved` is green: no journal entry carries both a `claim` and a `resolution`, and every entry bearing a `resolution` carries `author: \"umpire\"`. This is the mechanical half of ADR 0015 surviving — the GM cannot grade its own homework.",
                                "`uv run pytest tests/test_withheld_fields.py -k gm_view` is green offline: for every view the generator emits, every projected column resolves through `contracts.policy.column_disposition` to RENDERABLE; and a **synthetic** `rating-true` column planted into an in-test `Contracts` is proven absent from the emitted view SQL. The synthetic half is load-bearing — measured, the shipped `field_map.toml` holds 48 `identity`, 31 `structural`, 10 `rating-true` and **zero** `rating-scouted` fields, and all 10 `rating-true` entries are unclassified byte spans, of which exactly one (`bronze_name.name_category`) is landed. Without the synthetic case this test passes on a schema with nothing to withhold.",
                                "`uv run pytest tests/test_gm_grant.py::test_the_query_tool_accepts_no_connection_parameters` is green offline: the query tool\u0027s public entry point exposes no `host`, `user`, `password`, `database` or DSN parameter, asserted by signature inspection. This is what makes the GM\u0027s ability to `Read` the repo-root `.env` (measured: `.env` exists at the repo root, is gitignored at `.gitignore:4`, and the GM\u0027s `Read` grant is not path-scoped per `.claude/agents/README.md:104`) harmless — a credential it can read is a credential it cannot use.",
                                "`uv run pytest -m gamedata tests/test_gm_grant.py::test_the_restricted_user_reaches_gm_view_and_nothing_else` is green against the local warehouse: connecting as the restricted user, a `SELECT` against a `bronze_*` table raises a MySQL permission error, a `SELECT` against `MYSQL_TRUTH_REAL_DATABASE` raises one, `INSERT`/`UPDATE`/`DELETE`/`CREATE` against `gm_view` each raise one, and a `SELECT` against a `gm_view` view returns rows. The negative half alone would pass on a user with no grants at all.",
                                "`uv run pytest -m gamedata tests/test_gm_view.py::test_every_view_resolves_one_row_per_key_at_max_ingest_seq` is green: for each view, `COUNT(*)` equals `COUNT(DISTINCT \u003cdeclared bronze key minus ingest_seq\u003e)` per `(save_id, sim_date)`, and the `ingest_seq` each row carries equals `MAX(ingest_seq)` for its `(save_id, sim_date)`. This is the grain contract, enforced the same way `tests/test_grain_contracts.py` already enforces bronze\u0027s.",
                                "`uv run pytest tests/test_no_leaks.py` is green with the new tracked `gm/` files in scope (`.jsonl` is already in `scannable_text_files()`\u0027s keep set at `tests/test_no_leaks.py:85-98`), and `test_patterns_still_catch_real_leaks` is untouched and still green.",
                                "`uv run pytest tests/test_doc_links.py` and `tests/test_repo_structure.py` are green after the ADR set lands: every relative link in every tracked `.md` still resolves, and `docs/decisions/README.md`\u0027s status table lists 0013, 0016, 0018 and 0019 as superseded by the new ADR numbers.",
                                "`uv run pytest -m \"not gamedata\"` passes with no game install and no MySQL; `ruff check .`, `ruff format --check .` and `mypy` are clean.",
                                "**USER-RUN.** A cold session spawns the `gm` subagent with only its forced-read list and the query tool, asks one named baseball question, and the returned handoff\u0027s `## situation` attributes every figure to a query the tool logged — with the executed SQL present in the query log and no roster or club fact appearing in `## assumed`. The panel must not claim this one.",
                                "**USER-RUN.** The operator confirms `OOTP-AI.lg`\u0027s file set, sizes and modification times are unchanged after a GM invocation that queried, and that no `gm/` file\u0027s existing lines changed in the resulting diff. ADR 0001 and the append-only claim, checked by the one party that is not the code being audited."
                            ],
    "core_scope":  [
                       "**Phase A — retire the economy (no new code).** One ADR superseding 0013, 0018 and 0019, stating the hypothesis change: the independent variable is infrastructure-plus-adaptive-agent, and metered attention is a confound rather than a constraint. Carry forward, explicitly: 0018\u0027s retention-is-free reasoning (which the new model makes trivially true), 0019\u0027s feed-vs-warehouse insight (infrastructure can never replace what only the world knows — it becomes a *capability* statement rather than a pricing one), and 0019\u0027s refusal loop (which becomes the `declined` disposition in the execution log). Rewrite `FRONT_OFFICE.md`: delete `## The action economy`, replace the \u0027Pause time\u0027 row of the four-constraint table (`FRONT_OFFICE.md:31`), and rewrite `## What you are allowed to see`. Update `CLAUDE.md`\u0027s line naming 0016 as constraining what an agent may query. Update `docs/decisions/README.md`\u0027s status table.",
                       "**Phase A — the execution log.** Close `gm/ledger.jsonl` with a header note in `gm/README.md` (seq 1 stays where it is; its ruling was `free` under a retired model and rewriting it would falsify the one piece of history the repo has). Open `gm/execution-log.jsonl` at seq 1 with a declared schema: `seq`, `prev` (hash), `sim_date`, `what`, `proposed_by`, `disposition` in {executed, deferred, declined, superseded}, `reason`, `supersedes`. Document it in `gm/README.md` beside the existing ledger table. Rewrite `gm/standing-orders.md`\u0027s `## Reports` section, which currently prices commissioning at an action (`gm/standing-orders.md`, the Reports block).",
                       "**Phase A — the journal, one file.** `gm/journal.jsonl`, append-only, hash-chained, umpire-written from the GM\u0027s structured return. Envelope: `seq`, `prev`, `sim_date`, `author` in {gm, umpire}, `kind` in {note, trigger, claim, resolution}, `subject`, `resolve_by` (nullable sim date — set on `trigger` and `claim`), `refers_to` (nullable seq), `body`. One schema covers notes, dated triggers, pre-registered claims and their umpire-authored resolutions. Triggers carry a mandatory disposition on fire, recorded as a new entry (`refers_to` the trigger) — act, defer with a new `resolve_by`, or close with a reason.",
                       "**Phase A — the guards.** `tests/test_gm_memory.py`: the hash-chain integrity test, the trigger round-trip test, and the no-self-resolution test, all pure functions over fixture files so they run offline in CI. A small reader module (the selector the round-trip test exercises) is the only Python this phase needs. Resolve the `.gitattributes:24` collision: `merge=union` and a hash chain are incompatible — two branches appending both chain off the same `prev` and the merge is a silent fork — so the new files take no union driver, and a conflict is the honest outcome for a single-operator, single-club repo.",
                       "**Phase A — the agent-contract guard.** Extend `tests/test_agent_contract.py`, which today reads only `data-engineer.md` (`:18`), to parse `gm.md`\u0027s frontmatter and assert its exact `tools:` set with the dangerous grants named as forbidden. This lands in Phase A even though the grant does not widen until Phase B — it is the test that makes Phase B\u0027s widening reviewable.",
                       "**Phase A — the pinned-request dispositions.** One line each in `requests/feature-requests/gm-inbox/FEATURE_REQUEST.md` and `.../news-subscription-dial/FEATURE_REQUEST.md` status blocks, plus their Index rows at `requests/feature-requests/README.md:120` and `:124`, recording that both are priced in retired vocabulary and re-scope after this lands. Not a rewrite — both are still at `intake`, and scoping is where their vocabulary gets fixed.",
                       "**Phase B gate — the tool spike, and it is a gate, not a step.** Before any `gm_view` work, prove in a throwaway branch that a `gm`-shaped subagent can hold a query tool that (i) executes SQL against a restricted schema, (ii) takes no caller-supplied connection parameters, and (iii) is grantable in `tools:` frontmatter alongside `Read, Glob` without also granting `Bash`. Record the result with an epistemic label. **If the spike fails, Phase B does not ship and Phase A stands alone** — because a SQL grant delivered through `Bash` hands the GM `.env`, `players.dat`, the writable warehouse and `ootp_truth_real`, which is strictly worse than ADR 0016\u0027s prose wall.",
                       "**Phase B — `gm_view` generation.** Extend the `warehouse/ddl.py` pattern (`create_all_statements`, which already emits DDL from the declaration and refuses a vocabulary it has fallen behind at `:123-138`) with a view emitter that projects, for each of **four** tables — `bronze_player`, `bronze_team`, `bronze_team_roster`, `bronze_division_team` — exactly the columns `column_disposition()` returns RENDERABLE for, filtered to `MAX(ingest_seq)` per `(save_id, sim_date)`. That filter rule is not new: first-sight IMPLEMENTATION_PLAN.md:105 already settled it for reports (\u0027Reports resolve to `max(ingest_seq)` for the requested `sim_date` by default and state the seq they read on line one\u0027). History across sim dates stays fully visible — retention is free and hiding it recreates 0018\u0027s foresight trap. `bronze_name` resolves into the player view as `first_name`/`last_name` text rather than being served raw.",
                       "**Phase B — the grant.** A MySQL user with `SELECT` on the `gm_view` schema and nothing else: no bronze, no `ootp_truth_real`, no DDL, no DML. Documented in `ops/` as the setup step it is. Credentials never in a tracked file; the query tool injects them and exposes no connection parameter, so the GM\u0027s ability to `Read` the repo-root `.env` (measured present, gitignored, and unreachable by no path rule) becomes harmless rather than needing a fix.",
                       "**Phase B — the second ADR, and it is a separate decision.** Superseding 0016 and amending 0014 and 0017. 0014 narrows to sensor-versus-processing (the in-game sensor still cannot be improved by code; true-rating reconstruction stays foreclosed; analytics over scouted inputs becomes the point). 0017 keeps the GM a subagent and converts its no-DB foreclosure into a scoped grant, while its no-write foreclosure **stands unchanged** — the harness cannot enforce a path-scoped write and this ADR will not pretend otherwise.",
                       "**Phase B — the query log.** The tool appends every statement it executed to the execution log (or a sibling `gm/queries.jsonl`), written by the tool rather than by the GM. This is the audit backstop for anything the grant cannot reach, and it is also the only cost signal left once the action budget is gone — ADR 0013\u0027s Buys section named bounded token spend as one of its products, and removing the budget removes that bound."
                   ],
    "enhancements":  [
                         {
                             "title":  "Split the ADR set into exactly two, and land them in two commits",
                             "rationale":  "Open Question 6 asks one-or-several. Two, and the split is not stylistic: the information model (open the schema) depends on an unverified harness capability, while the memory model and the economy retirement depend on nothing. One ADR means the cheap two-thirds is hostage to the spike. Two means Phase A lands and stays landed even if Phase B is found unbuildable — which is the single most valuable property this scope can have, given that the spike genuinely might fail.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Hash-chain the append-only files instead of diffing against origin/main",
                             "rationale":  "Open Question 7 asks how to assert \u0027this file only grew\u0027 across branches with merge=union. The git-archaeology answer is fragile in four measurable ways: union merge interleaves rather than appends so a prefix test false-fails; union merge silently drops byte-identical duplicate lines; CI needs fetch-depth 0 to see the base; and the guard cannot run on main at all. A per-line `prev` hash makes tampering detectable inside the file with one offline pytest, no git, no branch semantics. It is strictly cheaper and strictly stronger, and it is what the request\u0027s own \u0027tamper-evident\u0027 actually asks for.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Write gm/charter.md as part of this work",
                             "rationale":  "It has been blocked as \u0027unwritten\u0027 since the club was created, and its own status note (`gm/charter.md:10-15`) names the blocker as \u0027the GM has no warehouse and no reports.\u0027 This request removes that blocker. Writing the charter is the concrete test of whether the new information model produces anything — and it makes the proposed `plan.md` visibly unnecessary, since the charter already carries a competitive window, operating principles, accepted constraints and a pre-registered falsifier with a decision-record supersession chain.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Log the SQL the GM executes, from inside the tool",
                             "rationale":  "Listed in the request as \u0027not now / later\u0027 under token instrumentation. Pull it into Phase B core instead: it is one append per query, it is the only remaining record of what the GM looked at once reports stop being the channel, and it is the audit surface that lets a reviewer notice a query the grant should have blocked but did not. Writing it from the tool rather than from the GM keeps it honest.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "A one-paragraph \u0027what the schema does not contain\u0027 note in the generated catalog",
                             "rationale":  "The request\u0027s Data Contracts section asks how a withheld column\u0027s absence is distinguished from a not-yet-landed one. Do not build a mechanism — first-sight Phase 11 already generates a withheld section with reason and ADR per field. One paragraph in that generator explaining that `gm_view` omits both kinds, and pointing at the section that distinguishes them, closes the question for the price of a sentence. Genuinely defer anything larger until a GM actually hits the ambiguity.",
                             "cost":  "cheap"
                         },
                         {
                             "title":  "Per-invocation token instrumentation",
                             "rationale":  "Worth having eventually — ADR 0013 explicitly counted bounded token spend among its products, and nothing replaces that bound. But instrumentation measures rather than bounds, and the smallest useful version needs a harness hook the repo does not have. File it separately once the query log has produced a season of evidence about what an unmetered GM actually costs.",
                             "cost":  "grows-build"
                         }
                     ],
    "risks":  [
                  "**The query tool may not be buildable as an enforced grant, and that is the load-bearing unknown.** `unconfirmed`. There is no `.claude/settings.json` in the repo (measured: `.claude/` holds only `agents/` and `skills/`), no MCP server, and no precedent for any subagent holding a tool outside `Read, Glob` and the builder\u0027s set. The only in-repo path to SQL is `Bash`, which simultaneously grants the GM `.env`, `players.dat`, the writable warehouse and `ootp_truth_real` — the answer key. A `Bash`-delivered grant would make the request\u0027s central claim (\u0027a fact about its connection, not a rule the GM follows\u0027) false in the most dangerous direction. This is why Phase B is gated.",
                  "**The GM can already read `.env` today.** Measured: `.env` exists at the repo root and is gitignored at `.gitignore:4`; `.claude/agents/README.md:104` states the harness has no path-level permission system, so the GM\u0027s `Read` grant reaches it. It holds `MYSQL_PASSWORD` and `MYSQL_TRUTH_REAL_DATABASE`. This is harmless today because the GM holds no tool that can use a credential — and becomes the single sharpest hole the moment it does. The mitigation (a tool with no connection parameters, plus a restricted user with no grant on the truth schema) is in core scope, but the hole is pre-existing and nobody has written it down before.",
                  "**The schema boundary currently protects almost nothing.** Measured from `src/ootp_ai/contracts/field_map.toml`: 48 `identity`, 31 `structural`, 10 `rating-true`, **zero** `rating-scouted`, zero `contract`. All 10 `rating-true` entries are unclassified byte spans (`unclassified_before_masks`, `name_category`, `name_usage_pairs`, `identity_to_status_spans`, …), and `tests/test_withheld_fields.py:309-317` records that exactly **one** of them is landed as a column. So \u0027the answer key is physically unreachable\u0027 is true of one byte of no consequence. Building the boundary now is correct sequencing — ADR 0016\u0027s own Context argues the ambiguity must close *before* the warehouse fills — but any acceptance test written over the shipped declaration is near-vacuous, which is why the synthetic planted-column case is mandatory rather than nice-to-have.",
                  "**There are no ratings to serve, and there may never be.** `docs/data-access.md:563-576` labels as `unconfirmed` the critical-path question of which file holds which rating view and *whether the scouted view is stored at all*, and states the failure mode outright: if OOTP computes it at render time, \u0027the front office would be able to read the answer key and nothing else.\u0027 ADR 0014\u0027s Costs section (`:71-74`) says the same. So a GM handed a query tool today can ask about names, ages, handedness, club assignment, roster-list membership and division structure — and nothing about how good anyone is. The request\u0027s framing (\u0027analytics over scouted inputs becomes the point\u0027) describes a capability that does not yet have a data path. This does not block the work; it does mean the operator should expect the first querying GM to be underwhelmed, and should not read that as the grant failing.",
                  "**Retiring a mechanism that has never been exercised.** `gm/ledger.jsonl` holds one entry; `gm/decisions/` holds one record; zero games have been played. ADR 0013\u0027s own Costs section says \u0027the starting numbers are guesses... nobody will know until a season has been played.\u0027 The request\u0027s grounds are a preference about which hypothesis is under test, which needs no data — but the *specific* pains cited (a flat budget starves bursty weeks, adjudication compounds) are predictions, not observations, and should be recorded as such in the superseding ADR rather than as findings.",
                  "**Grain fan-out in the views.** `bronze_team_roster`\u0027s grain is \u0027one row per player per team per roster list per save per snapshot\u0027 — a player appears on the active roster AND the 40-man AND the assignment list. A GM writing the obvious `JOIN bronze_player ON player_id` gets a silent 2–4× fan-out on any per-player aggregate. Measured from README.md:194: Boston at 2024-03-07 resolves to 33/26/30/7 across four lists for a ~40-man organization. The mitigation is not a rule the GM follows — it is that the roster view is pre-pivoted (one row per player per team with boolean list-membership columns) rather than served at its raw grain. Serving raw grain to a SQL-writing agent with no report author in between is exactly the failure the report wall incidentally prevented.",
                  "**`bronze_name` is 264,095 rows per save per snapshot.** Two saves are already landed. A `MAX(ingest_seq)` correlated subquery over that table, queried ad hoc by an agent, is a real cost and latency trap on a local MySQL. Excluding it from `gm_view` and resolving names into the player view is in core scope for this reason as much as for the withheld column.",
                  "**`merge=union` and a hash chain are incompatible, and `.gitattributes:24` currently sets union on the ledger.** Two branches appending both chain off the same `prev`; the merged file is a silent fork that the integrity test will (correctly) flag. Union merge also silently drops byte-identical duplicate lines, which is safe for seq-bearing JSONL and unsafe for anything else. The new files must take no union driver, and the collision must be stated in `gm/README.md` rather than discovered on the first two-branch week.",
                  "**MySQL view privilege semantics are `assumed`, not verified.** Views run with DEFINER rights by default, so a restricted user with `SELECT` on `gm_view` and nothing on bronze should read through fine — but `SHOW CREATE VIEW` needs `SHOW VIEW`, `information_schema` visibility is privilege-filtered in ways worth checking rather than asserting, and a view created by the app user inherits that user\u0027s reach. Verify each with an actual connection before writing the ADR\u0027s enforcement claim; the repo already learned once (CLAUDE.md\u0027s closing section) that an obviously-true MySQL belief was measurably wrong.",
                  "**Superseding four ADRs at once is the largest governance diff in the repo\u0027s history**, and `docs/decisions/README.md:6-7` says re-litigating a settled decision is the most expensive thing that can happen here. The mitigation is the two-ADR split plus the discipline the request already commits to (supersede, never delete) — but it means `FRONT_OFFICE.md`, `CLAUDE.md`, `gm/README.md`, `gm/standing-orders.md`, `.claude/agents/gm.md`, `docs/decisions/README.md` and two intake requests all move in step, and `tests/test_doc_links.py` resolves every relative link in every tracked `.md` with no fence exemption for `var/`. A single stale link turns CI red.",
                  "**first-sight is a *planned* request implemented through Phase 9 with 10–13 outstanding.** Reshaping its remaining phases mid-flight risks invalidating a cold-handoff plan that an adversarial panel already verified against real code. Shipping Phases 10–13 unchanged is the de-risking move, and it also preserves AC20 — the only end-to-end evidence the repo will have that a spawned GM produces a usable handoff, which is the baseline against which a querying GM should be judged.",
                  "**The plan-as-second-scorecard risk is real and only partly testable.** The request names it (0015 must stay the sole scorecard), but a GM that authors claims and a plan can always narrate success. The mechanical guard — resolutions are umpire-authored and never co-appended with the claim — catches the crudest version. The subtler version (a GM that only pre-registers claims it expects to win) is not catchable by CI and should be named as an accepted cost in the ADR rather than papered over with a test that cannot see it.",
                  "**Nothing bounds GM token spend once the budget is gone.** ADR 0013 counted this among its products (\u0027roughly 200 staff engagements per season rather than an open-ended optimization budget\u0027). An agent holding SQL and no budget can iterate indefinitely. The query log gives visibility but not a bound. Accept the unboundedness deliberately and in writing, or the first expensive week will read as a defect."
              ],
    "open_questions":  [
                           "Can a `gm`-shaped subagent hold a SQL tool in its `tools:` frontmatter without also holding `Bash`? This is the Phase B gate and everything expensive depends on it. `unconfirmed` — no precedent exists in the repo, and `.claude/agents/README.md:104-113` is explicit that only *which tools* is enforced, never *which paths*.",
                           "Does the restricted MySQL user\u0027s `SELECT`-on-`gm_view` grant actually deny `information_schema` visibility into bronze column names? Views hide values; they may not hide the existence of a withheld column\u0027s name. If a column *name* leaking is acceptable (it names a field, not a rating), say so; if not, the grant needs more than `SELECT`. `assumed` either way today.",
                           "Should `bronze_team_roster` be served pre-pivoted (one row per player per team, boolean list columns) rather than at its declared grain? Pre-pivoting prevents a silent fan-out but introduces the first place where `gm_view` reshapes rather than filters — and a reshaping view is a transformation layer, which is dbt\u0027s job under ADR 0005 and dbt is deferred (first-sight Phase 12 step 4).",
                           "Where does the query log live — a `disposition: \"query\"` row in the execution log, or a sibling `gm/queries.jsonl`? Volume argues for a sibling (queries will outnumber executions by orders of magnitude and would drown the execution record), but a second file is a second schema and a second guard.",
                           "Does `gm/journal.jsonl` carry a career or a club scope, per the table at `gm/README.md:44-49`? Triggers and claims about a specific roster are club-scoped; calibration record and method are career-scoped. The existing table would need a row either way, and `gm/README.md:51` says the directory does not split until a second club exists — so the answer is probably \u0027one file, scope recorded per entry,\u0027 but it should be decided rather than defaulted.",
                           "What happens to the `Established: ledger seq \u003cn\u003e` convention in `gm/standing-orders.md` once the ledger closes? Standing orders survive the economy\u0027s retirement (a policy staff apply is still useful), but their provenance field cites a file that stops growing. Re-point at the execution log, or drop the citation.",
                           "Is the operator willing to accept that the first querying GM sees no ratings at all — because `docs/data-access.md:563` is still `unconfirmed` on whether scouted ratings are stored — and to run a season on identity, roster and structural data alone? If not, the scouted-view spike (first-sight Phase 2) becomes a prerequisite of this request rather than a parallel one, and the sequencing changes.",
                           "If the Phase B spike fails, does Phase A still ship? Recommended yes, and the two-ADR split exists to make that possible — but the operator should confirm, because Phase A alone leaves the GM reading reports under a retired economy, which is a coherent but unglamorous end state."
                       ]
}
```

