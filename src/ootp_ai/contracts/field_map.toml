# Field map — what each landed field is, where it came from, and how well we know it.
#
# TRACKED, and legitimately so: a field map we computed is our observation, not Out of
# the Park Developments' data (ADR 0006, and `.claude/agents/data-engineer.md`: "A
# field-offset map you computed is ours and is tracked. A copy of players.csv is Out of
# the Park Developments' and is not").
#
# This file starts INCOMPLETE, by design and with the gap named rather than hidden:
#
#   * `teams.dat` landed in Phase 5 and its fields were never added here, because the
#     contracts directory did not exist yet. That backfill is Phase 8's, which the plan
#     describes as "complete tables.toml and field_map.toml".
#   * Only the `players.dat` fixed head is described below. The rest of the player
#     record sits past a drop-zero region that Phase 6b resolves.
#
# Nothing reads this file yet. `contracts/loader.py` and `contracts/policy.py` are
# Phase 8 and Phase 10; until they exist this is a declaration, not a gate.
#
# ── the vocabulary ───────────────────────────────────────────────────────────
#
# category   identity | rating-true | rating-scouted | contract | structural
#            An UNCLASSIFIED field is recorded `rating-true`, not "unknown". That is the
#            withhold-by-default posture ADR 0012 requires: "probably fine" is not a
#            classification, and the cost of guessing wrong on a rating is a confidently
#            wrong recommendation with nothing raised.
#
# epistemic  measured | verified | inferred | assumed | unconfirmed
#            `verified` here means scored against EVERY row of an independent answer key
#            and matching exactly — not sampled, not spot-checked.
#
# validator  which check produced the label, so a reader can re-run it.

[meta]
schema_version = 1
updated = "2026-08-17"
phase = "first-sight Phase 6a"
covers = ["players.dat"]
incomplete = ["teams.dat", "world.dat", "names.dat"]

# ── players.dat, the fixed head ──────────────────────────────────────────────
#
# Every `verified` row below was scored against all 18,072 `retired = 0` rows of
# `ootp_truth_real.players` and matched exactly. Evidence:
# `tests/test_parse_players.py::test_every_landed_field_matches_the_export_exactly`.

[[field]]
name = "player_id"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u32"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"
note = "Ascending across the file. OOTP's internal key, and the one that covers every player — unlike historical_id, which covers ~10.6%."

[[field]]
name = "name_indices"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u32 pair"
category = "identity"
epistemic = "unconfirmed"
validator = "none"
note = "Two u32s at record+4 and +8, both inside names.dat's ~264,095-entry range in every record sampled. Suggestive, not proof, and WHICH is first-name and which is last is not established at all. Phase 7 resolves them by brute force against a full answer key. Carried as an opaque pair so nothing downstream mistakes a position for a proof."

[[field]]
name = "date_of_birth"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "date (u8 day, u8 month, u16 year)"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"
note = "Load-bearing beyond its own value: the walk frames records by requiring this to agree with the age byte and the file's sim date."

[[field]]
name = "age"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u8"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"
note = "Stored, not derived — but measured to equal the whole years between date_of_birth and the sim date for all 18,072 records, with zero exceptions. That invariant is what makes record framing exact."

[[field]]
name = "nation_id"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u8"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"

[[field]]
name = "city_of_birth_id"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u32"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"
note = "1,140 distinct values across the sample, which is what makes a 100% match meaningful rather than a coincidence of low cardinality."

[[field]]
name = "weight"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u16"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"

[[field]]
name = "height"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u8"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"

[[field]]
name = "uniform_number"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u8"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"
note = "The plan's §2.5 example of a field neither scale-converted nor scout-filtered: 34 on the screen is 34 in the bytes."

[[field]]
name = "experience"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "u8"
category = "identity"
epistemic = "verified"
validator = "export-exact-all-rows"

[[field]]
name = "content_digest"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "string (u32 length + ASCII)"
category = "structural"
epistemic = "measured"
validator = "cross-save-comparison"
note = "A 64-character hex digest in the file preamble. Measured 2026-08-18: all three saves carry DISTINCT digests, and the two test saves agree on exactly the first 32 hex characters before diverging. Corrected — an earlier note claimed the test saves shared one digest, which came from comparing the first twelve characters of a debug print rather than the string. What the 32/32 split means is `unconfirmed`. Not a game fact; a property of the file."

# ── crossed but not classified ───────────────────────────────────────────────
#
# Bytes the walk consumes inside the fixed head and does not interpret. Recorded as
# `rating-true` / `unconfirmed` per the withhold-by-default rule, so that if any of them
# turns out to be a rating it is already being withheld rather than already shipped.

[[field]]
name = "unclassified_after_birth_date"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "3 bytes"
category = "rating-true"
epistemic = "unconfirmed"
validator = "none"
note = "record+16..+18. Zero in every record inspected."

[[field]]
name = "unclassified_after_age"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "1 byte"
category = "rating-true"
epistemic = "unconfirmed"
validator = "none"
note = "record+20."

[[field]]
name = "unclassified_after_nation"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "5 bytes"
category = "rating-true"
epistemic = "unconfirmed"
validator = "none"
note = "record+22..+26. The widest unclassified span in the head."

[[field]]
name = "unclassified_after_height"
source = "players.dat"
walker = "ootp_ai.parser.players.read_players"
type = "1 byte"
category = "rating-true"
epistemic = "unconfirmed"
validator = "none"
note = "record+34."

# ── deliberately NOT landed ──────────────────────────────────────────────────
#
# Recorded because their absence is a decision, not an oversight. A later reader
# otherwise has no way to tell "nobody got to it" from "this was refused".

[[withheld]]
name = "ratings"
source = "players.dat"
reason = "ADR 0012, permanent. Two lossy transforms sit between a stored rating and any display — scale conversion and scout filtering — so matching a displayed value to a byte identifies the wrong field with no error surfaced. This slice lands no ratings whatever the Phase 2 spike returned."

[[withheld]]
name = "historical_id"
source = "players.dat"
reason = "NOT LANDED, and this entry exists because it nearly went unrecorded. It is named by plan Phase 6 step 1 and it is the join key AC8 depends on — the only Tier-A validation of the names join on the league we actually manage — so its absence is the most consequential gap in this phase and the easiest to miss. Measured: it is a u32-length-prefixed ASCII string (e.g. 'deverra01') appearing TWICE per record, roughly 60 to 80 bytes after the record start, which places it AFTER the drop-zero region and therefore out of reach of this walk for the same reason team_id is. Landing it is Phase 6b's, and AC8 cannot be attempted until it exists. Carrying ~10.6% coverage (1,920 of 18,072), it is a nullable attribute and never a join key in a serving path."

[[withheld]]
name = "team_id, organization_id, league_id, position, role, bats, throws"
source = "players.dat"
reason = "Not refused — not yet readable. They sit past the drop-zero region that begins after `experience`, where a field whose value is falsy is omitted entirely. Measured: reading team_id at a constant offset scores ~87%, because last_team_id is dropped for players who never changed clubs and everything after it shifts four bytes. An 87% field is the failure the fixed-offset ban exists to prevent. Phase 6b resolves the region."
